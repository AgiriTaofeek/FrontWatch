# FrontWatch — Build Progress

A personal build-tracking checklist, kept separate from `docs/`. The *plan* lives in [`docs/02-product/roadmap.md`](docs/02-product/roadmap.md) §2 and [`docs/07-delivery/execution-roadmap.md`](docs/07-delivery/execution-roadmap.md) — this file tracks actual status against that plan, plus notes on anything that changed along the way. **Order isn't fixed** — if a step gets reordered or reshaped, note it in the deviations log below rather than silently rewriting history.

Legend: `[ ]` not started · `[~]` in progress · `[x]` done

---

## Step 1 — Repository & local platform ✅

- [x] Monorepo skeleton (`apps/`, `packages/`, `services/data-plane/`) per `docs/06-engineering-specs/README.md`
- [x] Bun workspace root (`package.json` + `workspaces`), one placeholder `package.json` per package
- [x] Go module for `services/data-plane`, four `cmd/*` binary stubs (`ingestion`, `worker`, `alert-worker`, `retention-worker`), `go build ./...` compiles clean
- [x] `.gitignore` covers Go build artifacts (`bin/`, `*.test`, `*.out`)
- [x] Local infra verified healthy (`infra/local/docker-compose.yml` — Postgres, ClickHouse, Redpanda)
- [x] Initial commits pushed to `github.com/AgiriTaofeek/FrontWatch`, default branch `main`
- [x] CI (`.github/workflows/ci.yml`): Biome check + Go build/lint on push and PR
- [x] Branch protection on `main`: PR required (0 reviewers, solo project), CI must pass, no force-push/delete, applies to admins too

**Done:** 2026-08-12 — this fully closes M0's "CI" exit criterion, which had been marked done prematurely in the previous entry.

**Workflow from here on:** branch → PR → CI green → self-merge. Direct pushes to `main` are no longer possible (protection is enforced, including for the repo owner).

---

## Step 1.5 — Engineering standards & tooling *(decided 2026-08-12, before Step 2 code starts)*

- [x] Router for `apps/control-api`: **Elysia** — [ADR-018](docs/decisions/ADR-018-elysia-control-plane-router.md)
- [x] TS/Bun lint + format: **Biome**, replacing ESLint+Prettier — [ADR-019](docs/decisions/ADR-019-biome-ts-tooling.md)
- [x] Go lint: `golangci-lint` (unchanged, decided independently)
- [x] Auth library decision **deferred to Step 9** — do not pull in better-auth or wire up Keycloak/OIDC during Step 2; `tech-stack.md` already names OIDC/Keycloak as the documented direction, revisit deliberately (possible ADR) if that changes
- [x] Biome + golangci-lint installed and verified working (`biome check .` clean, `golangci-lint run ./...` clean)
- [x] Elysia installed in `apps/control-api`, `@types/bun`, shared `packages/config/tsconfig.base.json`
- [x] CI workflow + branch protection on `main` (recorded under Step 1 above — CI applies repo-wide, not control-plane-specific)
- [x] Husky + commitlint (`commit-msg`, enforcing Conventional Commits) + lint-staged (`pre-commit`: Biome on staged TS/JSON, `go build`+`golangci-lint --new-from-rev` on staged Go)

**Done:** 2026-08-12

---

## Step 2 — Control plane *(minimal — project identity only, no auth/RBAC yet)*

- [x] DB tooling decided: **Drizzle ORM, `bun-sql` driver** — [ADR-020](docs/decisions/ADR-020-drizzle-bun-sql.md)
- [x] UUID primary keys decided — [ADR-021](docs/decisions/ADR-021-uuid-primary-keys.md)
- [x] `drizzle.config.ts` + schema file in `apps/control-api` (`src/db/schema.ts`)
- [x] `projects` table (Postgres) — FKs to `application_id`/`environment_id` left **nullable for now** (real model in `data-model.md` §1 requires them; full `Application`/`Environment` tables deferred, tracked shortcut not silent scope creep)
- [x] First migration generated + applied against local Postgres (`infra/local/docker-compose.yml`), verified via `psql`
- [x] A way to create a project and read back its ID — `POST /projects` + `GET /projects/:id` (`apps/control-api/src/routes/projects.ts`)
- [x] Tests written alongside this code — `projects.test.ts`, integration-layer (real local Postgres), CI runs them against a `postgres:16` service container
- [x] `apps/control-api/src/index.ts` — app entry point mounting the route

**Done:** 2026-08-12 — closes Step 2. No auth/RBAC yet (still correctly deferred to Step 9).

**Why here:** the SDK's `init()` and every telemetry event need a real `project_id`, not a placeholder — see the reasoning captured in conversation on 2026-08-12.

---

## Step 3 — SDK skeleton ✅

- [x] `init()` — config validation (never crashes on bad config, disables itself instead), dedup behavior, client creation (`client.ts`)
- [x] Context (release, environment, route, sessionId, userAgent — minimal, no UA parsing yet) (`context.ts`)
- [x] Event envelope creation — SDK-side shape only, no tenant IDs (those get resolved server-side at ingestion, per data-model.md §10) (`event.ts`)
- [x] Buffer (bounded, FIFO, count-based only — no byte-size bound or shutdown-flush yet) (`buffer.ts`)
- [x] Transport skeleton — batching, bounded exponential backoff, retries only 5xx/network failures not 4xx (`transport.ts`)
- [x] Privacy stub — no-op, but structurally present in the pipeline per ADR-007 (`privacy.ts`)
- [x] First real instrumentation: `errors.ts` — `window.addEventListener('error'/'unhandledrejection')`, thin wrapper over `captureException`
- [x] Tests: 11 passing (buffer, transport retry/backoff with mocked fetch, error-instrumentation with `happy-dom`)

**Done:** 2026-08-13. **Real finding:** a root-level `bun test` does not reliably apply a nested package's `bunfig.toml` (confirmed empirically — `happy-dom` never registered, `window is not defined`) — each package's tests must run scoped to that package, both locally and in CI. Fixed `ci.yml` accordingly (separate `apps/control-api` and `packages/sdk` test steps).

**Deferred, not forgotten:** resource-loading-failure capture, `captureMessage`/`addBreadcrumb`/`setUser`/`flush`/`close`, sampling, real UA parsing, byte-bounded buffer, shutdown flush, framework adapters.

---

## Step 4 — Ingestion

- [x] ADR-022: Go ingestion reads Postgres directly (read-only), resolving code-structure.md's explicitly-flagged open question
- [x] `packages/contracts`: wire types matching `api-contracts.md` §3-4 exactly (`WireEvent`, `IngestRequest`, `IngestResponse`), plus a fixture lifted straight from the doc's own example
- [x] Fixed a real drift: SDK's `transport.ts` was sending an invented shape (camelCase, no batch envelope, made-up `X-Frontwatch-Key` header) that didn't match the documented wire contract at all — now serializes via `packages/sdk/src/serialize.ts` → `@frontwatch/contracts`, standard `Authorization: Bearer <publicKey>`, `POST /ingest/v1/events`
- [x] Contract test: `serialize.test.ts` asserts the serializer's output against the spec-derived fixture directly, not just "it compiles"
- [x] Validate incoming payload against the versioned event contract (Go side) — `internal/telemetry`, pure domain layer, zero infra imports per code-structure.md's strict dependency direction. 7 table-driven tests passing, `golangci-lint` clean
- [x] `internal/platform/config` — typed startup config, exits clearly on missing `DATABASE_URL`/`REDPANDA_BROKERS` rather than starting partially-configured. Real finding: a test passed locally only because my shell had `DATABASE_URL` exported from a prior command — `t.Setenv` doesn't clear vars it wasn't told about — fixed by explicitly clearing it per-subtest instead of relying on ambient absence
- [x] Auth against project key (Go side) — `internal/storage/postgres`, one narrow `ProjectCredentialRepository.FindActiveByPublicKey`, `pgx` v5.10.0, per ADR-022. Disabled and nonexistent keys deliberately return the same error (no signal leaked either way). Integration-tested against real local Postgres (insert/query/cleanup), skips cleanly if `DATABASE_URL` unset
- [x] Real gap caught and fixed: `ci.yml`'s Go job only ever ran `go build` + `golangci-lint` — `go test` never ran in CI, so the Postgres integration test above would have passed locally forever and never actually run where it matters. Added a `postgres:16` service + Bun/drizzle-kit migration step to the Go job too (Go's tests need the real schema Bun owns)
- [x] Publish to Redpanda (`franz-go` v1.21.6) — `internal/queue`, publishes raw decoded events to `telemetry.raw`, keyed by `project_id` (per-project ordering, load spread across partitions). Normalization/fingerprinting stay downstream in the worker, not ingestion's job
- [x] **Real infra bug found and fixed**: `infra/local/docker-compose.yml`'s Redpanda only advertised itself at `redpanda:9092` (Docker-internal hostname) — any client running on the host (not in a container) could complete the initial bootstrap against `localhost:9092` but then hung trying to reach the *advertised* address for the actual produce/consume. Never caught before because the healthcheck runs `rpk` *inside* the container, and nothing had actually tried to produce/consume from the host until this. Fixed with a proper dual-listener config (internal for other containers, external on a distinct port for the host) — confirmed by hitting `context deadline exceeded` before the fix, sub-100ms after
- [x] CI: `services:` can't override a container's startup command (needed for Redpanda's required flags) — tried the official `redpanda-data/github-action`, found it genuinely broken (hardcoded to a Docker Hub image name — `vectorized/redpanda` — that no longer exists), replaced with a direct `docker run` using the same known-working image as local dev
- [x] `internal/telemetry/service.go` — `IngestService` orchestrating `Authenticate → Validate → durably enqueue`, matching `api-contracts.md` §3 exactly, including partial batch acceptance (one malformed event rejects only that event, not the whole batch). 5 tests with fake credential/publisher implementations
- [x] `cmd/ingestion/main.go` wired together for real — HTTP handler (thin, per `services.md`'s rule: parse + map response, no business logic), `Authorization: Bearer` parsing, graceful shutdown (SIGTERM → stop accepting → finish in-flight → close dependencies, bounded 10s timeout, per `operations.md`)
- [x] **Full end-to-end verification, not just unit tests**: real project created in Postgres, real server started, real `curl` requests covering all paths (valid batch, missing auth → 401, wrong key → 401, malformed body → 400, mixed valid/invalid batch → partial acceptance) — then confirmed the accepted events actually landed on Redpanda with correct content via `rpk topic consume`, and that the rejected event correctly never did

**Done:** 2026-08-13 — closes Step 4. The MVP golden scenario's first three hops (SDK → ingestion → Redpanda) are now real and verified end to end, not just individually tested.

**Deferred, not forgotten:** `/health/live`/`/health/ready` endpoints (operations.md names these explicitly — Step 8's job), ingestion metrics (`ingestion_requests_total` etc., also Step 8), rate limiting, privacy checks at the ingestion boundary (instrumentation.md mentions this but it's not built), topic provisioning (currently relies on Redpanda's auto-topic-creation — fine for MVP, not a production answer per ADR-013).

---

## Step 5 — Processing

- [x] **Scope clarified**: issue grouping/counts/the Issue entity itself are Step 6's job, not Step 5's — this step is consume → decode/validate → normalize (pass-through, no framework-specific shapes exist yet to normalize) → fingerprint → write raw events, no mutable state
- [x] `migrations/clickhouse/0001_create_events_table` — `golang-migrate` (v4.19.1) with the ClickHouse driver, same versioned/reviewable role `drizzle-kit` plays for Postgres. `events` table: `ReplacingMergeTree`, partitioned by `toDate(client_timestamp)` (data-model.md §7's literal recommendation), ordered by `(project_id, event_type, client_timestamp, event_id)`
- [x] **Real infra bug #3 found and fixed, same class as Redpanda's**: the official ClickHouse image disables network access entirely for the `default` user unless `CLICKHOUSE_USER`/`CLICKHOUSE_PASSWORD` is set — confirmed directly from its own startup log. ClickHouse had been unreachable from outside its own container this whole time; the `/ping` healthcheck never required auth so never caught it
- [x] `internal/issue/fingerprint.go` — SHA-256 of `exception_type + normalized_message`, truncated to 16 hex chars, versioned (`FingerprintVersion`) per the legacy fingerprinting spec's explicit requirement. Normalization strips numbers/UUIDs/hex/quoted strings — noise-resistance verified both in unit tests and end-to-end (two real events with different order IDs through the whole running pipeline produced the identical fingerprint)
- [x] `internal/storage/clickhouse` — `EventWriter`, `clickhouse-go` v2.48.0 (official client)
- [x] `internal/telemetry/process_service.go` — `ProcessEventService` (Normalize→Fingerprint only; persist is the worker's wiring concern, not this service's)
- [x] `cmd/worker/main.go` wired together — `franz-go` consumer group, manual per-record commit strictly in order (a plain loop, not `EachRecord`'s callback — needed the ability to `break` early on a persist failure, since Kafka's committed offset is a single watermark per partition, not a sparse set; committing a later record would have silently marked an earlier failed one as done too)
- [x] **Full end-to-end verification**: real project created, both ingestion and worker started for real, sent real events through `curl` → ingestion → Redpanda → worker → ClickHouse, confirmed via direct ClickHouse queries. Also verified the explicit "malformed event must never terminate the worker" requirement by publishing genuinely broken JSON straight onto the topic — logged clearly, worker kept running

**Done:** 2026-08-14 — closes Step 5. **Deferred, not forgotten:** bounded-concurrency record processing (currently sequential — simplest-correct, not yet a throughput bottleneck), retry-with-backoff before giving up on a persist failure (currently stops and waits for redelivery/restart), dead-lettering malformed/invalid records (currently logged and skipped), enrichment (browser family/device class — blocked on the same UA-parsing gap noted in Step 3), a second server-side privacy layer (instrumentation.md names this; neither layer is real yet, client or server).

---

## Step 6 — Error investigation *(closes the MVP build-gate slice)*

- [x] ADR-023: issues derived by query (`GROUP BY project_id, fingerprint` over `events`), no separate materialized table — resolves the Step 5 deferral. Resolution state (`E11.03`) explicitly out of scope, not in `PROGRESS.md`'s original Step 6 list or `mvp.md`'s golden scenario
- [x] ADR-024: TanStack Start for `apps/web` — resolves `tech-stack.md`'s open frontend-router question, also doubles as SDK dogfooding (Tier 1 adapter target)
- [x] Issue grouping — `apps/control-api/src/db/issues.ts`, the ADR-023 aggregation queries (`listIssues`, `getIssue`), verified against `@clickhouse/client` v1.23.1 (works fine under Bun)
- [x] Issue API (Bun control plane) — `GET /api/v1/projects/:projectId/issues`, `GET /api/v1/issues/:issueId`. **Deviation from api-contracts.md** (tracked, not silent): documented as `/applications/{id}/issues`, built as project-scoped instead — `Application` isn't a real entity yet (Step 2's nullable-FK shortcut), `project_id` is what `events` is actually keyed by
- [x] **Also fixed while wiring this up**: every control-plane route lacked the `/api/v1` prefix `api-contracts.md` documents (ingestion already had `/ingest/v1/`, this side never got the equivalent) — fixed at composition time in `index.ts`, not per route file, so route-level tests keep testing unprefixed paths directly
- [x] **Real bug caught only by starting the actual composed server**, not by either route file's isolated tests: Elysia's router rejects two different parameter names (`id` vs `projectId`) at the same URL position across merged route trees — `projects.ts`'s `/projects/:id` renamed to `/projects/:projectId` for consistency with `issues.ts`
- [x] **Real format gap caught by testing**: ClickHouse's `DateTime64` via JSONEachRow insert wants its own `YYYY-MM-DD HH:MM:SS.mmm` string format, not ISO-8601 with `T`/`Z` — didn't surface in Step 5 because Go's client uses the binary native protocol, not string parsing
- [x] **Full end-to-end verification through all three services**: real project created, ingestion + worker + control-api all started for real, a `curl` request matching `mvp.md` §3's exact golden scenario (release `4.2.0`, route `/transfer`) sent through ingestion → Redpanda → worker → ClickHouse → confirmed visible via the real issue list and issue detail endpoints
- [x] CI: added ClickHouse to the TS job too (separate runner from the Go job, needs its own migrated instance) — the migration step there needs `golang-migrate` (Go-based) even though it's nominally the "TypeScript" job, since that's the one tool this project uses for ClickHouse schema
- [x] `apps/web` bootstrapped for real — TanStack Start (ADR-024), first frontend code since Step 1. `@tanstack/react-query` as the "dedicated query layer" `application-architecture.md` requires, query keys include every filter (`["issues", projectId]`), 30s `staleTime` per its "cache short-lived, telemetry isn't immutable" rule
- [x] `packages/contracts/src/issues.ts` — the Issue API's response shapes shared between `control-api` and `web`, same lesson Step 4 already paid for once with the SDK/ingestion wire contract: one definition, not two that can quietly drift
- [x] Dashboard issue list — `/projects/$projectId/issues`, real server-rendered data (TanStack Start streamed the `useSuspenseQuery` result into the initial HTML with zero explicit loader wiring — confirmed by reading the raw SSR'd HTML, not assumed)
- [x] Dashboard issue detail — `/issues/$issueId`, links directly from the list
- [x] **Two real setup bugs found only by actually starting the dev server**, not by writing the code: (1) `vite.config.ts` failed to load — `@tanstack/react-start/plugin/vite` is ESM-only but was being `require()`'d, because `apps/web/package.json` was missing `"type": "module"` (lost when `bun add` populated it, present in the setup guide's example); (2) `packages/contracts/package.json` had no `main`/`types` field at all — a latent gap since Step 1 that only became a visible error once a second package (`web`, alongside `sdk`) started importing from it
- [x] Component tests too, not just E2E — `@testing-library/react` + `happy-dom` (same scoped-`bunfig.toml` pattern as `packages/sdk`). Found two real bugs in the tests themselves while writing them: `<Link>` needs actual `RouterProvider` context to render (a bare `QueryClientProvider` wrapper threw), and `findByText` throws instead of returning falsy when nothing matches (need `queryByText` for "should NOT be present" assertions)
- [x] **Full end-to-end verification through all four services**: real project, real `curl` matching `mvp.md` §3's exact golden scenario, confirmed by reading the actual server-rendered HTML from the running dashboard — both the issue list and issue detail pages, real data, no gaps
- [x] CI: `apps/web` gets its own scoped test step (same root-vs-nested `bunfig.toml` limitation from Step 3)

**Milestone: Step 6 is done. `mvp.md` §3's golden scenario is now real, end to end** — a browser error becomes a queryable, visible issue in a real dashboard, verified with real services and real HTTP requests at every layer, not simulated. This closes the MVP build-gate slice from `mvp.md` §1.

---

## Step 7 — Context

- [~] Network — **SDK + Go pipeline done**, dashboard surface not yet
  - [x] `packages/contracts` + SDK `event.ts`: `EventType`/`WireEventType` extended to a real discriminated union (`error` | `network`) — the refactor Step 4's own comment said to do "when a second event_type is real"
  - [x] `packages/sdk/src/network.ts`: automatic `fetch` capture. Resource normalization mirrors the backend fingerprint normalization's *idea* (strip numeric/UUID segments, drop query strings) in a different language — `/api/users/123` → `/api/users/:id`. XHR instrumentation deferred, not forgotten (materially different API shape, real separate work)
  - [x] **Real gap caught while adding this**: `core-architecture.md` says `init()` is supposed to "register instrumentation" itself — ours never did, `registerErrorInstrumentation()` had to be called manually this whole time. Fixed by auto-registering both error and network instrumentation inside `init()`, with a dedicated registration guard (separate from `client.ts`'s own duplicate-init guard) so a second `init()` call can't double-wrap `window.fetch`
  - [x] **Real architectural constraint respected, not worked around**: `errors.ts`/`network.ts` both import from `client.ts`; auto-registration logic lives in `index.ts` (not `client.ts`) specifically to avoid a circular import that moving it into `client.ts` would have created
  - [x] Never captures the SDK's own telemetry requests as network events (would otherwise generate infinite low-value "network event about sending network events" noise) — `ignoreUrlPrefix` excludes the configured ingestion endpoint
  - [x] **Real Bun testing gotcha found and documented**: `mock.module()` replaces a module in the *shared* registry for the whole test run, not scoped per file — `errors.test.ts`'s mock of `./client` (only stubbing `captureException`) broke `network.test.ts` when run together, since the mocked module didn't have `captureNetworkEvent`. Fixed by keeping every file's mock of a shared module complete, not just what that one file uses
  - [x] 24 sdk tests passing (was 13), including 3 full-suite runs to confirm the mock-ordering fix actually holds, not just once
  - [x] Go `internal/telemetry.Event`: `Payload ErrorPayload` (a single concrete struct) replaced with a real discriminated shape — `ErrorPayload *ErrorPayload` / `NetworkPayload *NetworkPayload`, exactly one set per `EventType`, mirroring the TS union. `encoding/json` can't pick a struct type from a sibling field on its own, so `Event` grew its own `MarshalJSON`/`UnmarshalJSON` (shared via one `eventWire` struct) rather than pushing every consumer into runtime type assertions
  - [x] `Event.Validate()` now branches per `EventType`: error events require `payload.message`, network events require `payload.method` + `payload.resource`; a genuinely unsupported `event_type` (e.g. `performance`) is still rejected the same as before
  - [x] `ProcessEventService.Process()` only fingerprints error events (unchanged behavior, now guarded on `event.ErrorPayload != nil` too); `EventWriter.WriteEvent()` uses a new `Event.PayloadJSON()` helper instead of assuming one payload shape — same helper backs both `MarshalJSON` and the ClickHouse write path, so the switch-on-`EventType` logic exists in exactly one place
  - [x] **Real end-to-end verification, not just unit tests**: ran ingestion + worker against the real local Postgres/Redpanda/ClickHouse, `curl`'d a real network event through `/ingest/v1/events`, confirmed it landed in ClickHouse with the correct `payload` JSON via a direct HTTP query — plus a regression check that error events and genuinely-unsupported `event_type`s still behave exactly as before
  - [x] **Real bug found during that E2E pass, not a code bug**: stale `go run ./cmd/worker` processes left running from earlier Step 5/6 testing were still members of the `telemetry-worker` consumer group, running the *old* pre-network-support code. Since the dev topic has one partition, one of those old processes silently won the assignment, decoded the network event with the old single-`ErrorPayload` shape (getting zero values, not a decode error), failed `Validate()`, and committed past it as a permanent rejection — so the first E2E attempt showed nothing in ClickHouse with no error logged anywhere. Found by checking `ps aux` and the raw Redpanda topic (`rpk topic consume`) directly rather than assuming the new code was wrong; fixed by killing every stale process before retesting
  - [x] New tests: `TestEvent_JSONRoundTrip` (error + network, both directions, plus a raw-JSON shape check), `Validate()` table cases for both payload types, `ProcessEventService` skips fingerprinting for network events, `TestEventWriter_WriteEvent_NetworkEvent` (real ClickHouse integration test), `IngestService` accepts a valid network event — all passing, `golangci-lint run ./...` clean
  - [x] Dashboard surface: `GET /projects/:projectId/network` (control-api) aggregates by `(method, resource)` — same ADR-023 GROUP-BY-over-raw-events shape as issues, no materialized table. `duration_ms`/`outcome` live inside the raw `payload` JSON string column (ADR-008), pulled out per-row with ClickHouse's `JSONExtractString`/`JSONExtractFloat` since the aggregation (count/quantile) has to run in ClickHouse anyway — request count, failure count/rate, p50/p95 duration via `quantile()`
  - [x] `packages/contracts/src/network.ts` — same shared-contract pattern as `issues.ts`, one definition for control-api + web
  - [x] `apps/web`: `/projects/:projectId/network` route + `NetworkList` — same `useSuspenseQuery` pattern as `IssueList`, no per-resource detail page (deliberately deferred — no natural drill-down target the way an issue has occurrences). Small cross-links added between the Issues and Network pages since both are now real
  - [x] **Real end-to-end verification, full stack**: ran ingestion + worker + control-api + the actual `vite dev` dashboard together (not mocked at any layer), sent 4 real network events (3 success, 1 failure, varied durations) through `/ingest/v1/events`, confirmed the aggregation via a direct `curl` to the new endpoint, then confirmed the same numbers rendered in the real server-rendered HTML from the running dashboard (25.0% failure rate, 103ms p50, 271ms p95 — matching hand-computed values for the 4 seeded events)
  - [x] New tests: `listNetworkResources` (groups by method+resource, keeps different methods on the same resource separate, ignores error events, respects a route filter, empty-project case) against real ClickHouse; route-level tests for `GET /projects/:projectId/network`; `NetworkList` component tests (empty state, row rendering, separate rows per method) — all passing, `golangci-lint`-equivalent (`biome check` + `tsc --noEmit`) clean for both `apps/control-api` and `apps/web`
- [x] Session — SDK lifecycle, control-api views, and dashboard, all done
  - [x] `packages/sdk/src/context.ts`: session id goes from "a random UUID for the SDK's in-memory lifetime" to a real lifecycle — persisted in `sessionStorage` (survives a page reload within the same tab, which a pure module-level variable never did) and expires after 30 minutes of inactivity (industry-standard default, e.g. GA4/Sentry both use this), extended on every event rather than fixed at session start. Multi-tab is still explicitly out of scope — `sessionStorage` is per-tab by design, an acceptable approximation, not solved here
  - [x] Graceful fallback chain, never throws: storage genuinely unavailable (SSR, disabled) → in-memory fallback identical to the old skeleton behavior; storage accessible but `setItem` throws (Safari private browsing) → same in-memory fallback for the rest of the page load; corrupted/malformed stored JSON → treated as no session, a new one is created
  - [x] **Real, previously-invisible gap found and fixed while verifying this**: `packages/sdk` had *no* `@types/bun` dependency at all — `tsconfig.base.json` requires `"types": ["bun"]`, but nothing actually provided it, and it was never caught because CI only ever runs `bun test` (which strips types and doesn't check them) with no `tsc --noEmit` step anywhere. `packages/contracts` had the identical gap; `apps/control-api` had it declared individually; `apps/web` sidesteps it entirely via its own `"types": ["vite/client"]` override. Fixed by hoisting `@types/bun` to the root `devDependencies` once (matching how `biome`/`husky`/`lint-staged` are already root-only) instead of duplicating it per package, and removing `control-api`'s now-redundant individual copy
  - [x] **Two more real type errors surfaced the moment `@types/bun` actually resolved for `packages/sdk`** (both pre-existing since the Network PR, #15, invisible until now): `network.test.ts`'s untyped `mock(() => {})` made `.mock.calls` infer as `[][]` (empty tuples), silently typing every `call?.[0]` as effectively unreachable — fixed by typing the mock's parameter; `network.ts`'s `window.fetch = async (...) => {...}` stopped structurally matching `typeof fetch` once `@types/bun`'s Bun-specific `fetch.preconnect` augmentation became visible (real in Bun's own runtime, meaningless for a browser SDK) — fixed with a documented `as typeof fetch` cast
  - [x] **CI gap closed as a direct consequence**: added a `Typecheck` step per TS package (`contracts`, `sdk`, `control-api`, `web`) to `ci.yml`, right after Biome — the exact step whose absence let all of the above stay invisible this entire session
  - [x] New `context.test.ts` (context.ts had no tests at all before this): session id creation, reuse within the timeout window, persistence in `sessionStorage` (not just in-memory), activity-extends-the-session, expiry just past 30 minutes, no-expiry just under 30 minutes, recovery from corrupted stored JSON, and the full in-memory-fallback path (storage access itself throwing) — 9 new tests, 33 total in `packages/sdk` (was 24)
  - [x] `GET /projects/:projectId/sessions` + `GET /sessions/:sessionId` (control-api): same ADR-023 GROUP-BY-over-events shape as issues/network, no materialized Session table — `session_id, started_at, last_seen_at, event_count, error_count, network_count, first_route, last_route` via `min`/`max`/`countIf`/`argMin`/`argMax`, plus a chronological (oldest-first, unlike issue occurrences' newest-first) timeline query for detail, with a server-built human-readable `summary` per event (error message, or `METHOD resource -> status` for network). `browser`/`device` from `data-model.md`'s Session entity still aren't populated — needs real User-Agent parsing, which `context.ts`'s own comment already documents as deliberately not built
  - [x] Session id is a `"{projectId}:{session_id}"` composite (same pattern as `IssueSummary.issueId`) so `/sessions/:sessionId` is a flat, self-describing resource — not nested under project, matching the issue detail route's shape
  - [x] `apps/web`: `/projects/:projectId/sessions` (`SessionList`) + `/sessions/:sessionId` (`SessionDetail`, full timeline table). Three-way cross-nav added between Issues/Network/Sessions now that all three are real
  - [x] **`session-investigation.md`'s documented entry point wired up for real**: "Issue → affected session" — `IssueDetail`'s occurrence rows now link to `/sessions/:sessionId`, built client-side from the issue's own `issueId` prefix (`"{projectId}:{fingerprint}"`, split on the first colon) + the occurrence's raw `sessionId`, so no contract shape needed to change just to carry `projectId` separately
  - [x] **Real end-to-end verification, full stack**: ran ingestion + worker + control-api + the actual `vite dev` dashboard together, sent an error event and a network event sharing one `session_id` through `/ingest/v1/events`, confirmed the session list/detail endpoints via direct `curl`, then confirmed the identical numbers and full timeline (both events, correct order, correct summaries) rendered in the real SSR'd HTML — including following the Issue → session link end to end and confirming its `href` was exactly `{projectId}%3A{sessionId}`
  - [x] New tests: `listSessions`/`getSession` (groups error+network events on one session, chronological timeline with correct summaries, excludes events with no `session_id`, empty-project case) against real ClickHouse; route-level tests for both endpoints; `SessionList`/`SessionDetail` component tests; extended `IssueDetail`'s tests for the new session link (dash when absent, correct scoped `href` when present) — all passing, `biome check` + `tsc --noEmit` clean across all 4 TS packages
- [x] Performance — SDK, Go pipeline, and dashboard, all done
  - [x] `packages/sdk/src/performance.ts`: wraps the official `web-vitals` (v6.1.0) library's `onCLS`/`onFCP`/`onINP`/`onLCP`/`onTTFB` — instrumentation.md §Performance's first five metrics. Deliberately a thin adapter, not a reimplementation: `instrumentation.md`'s own hard requirement ("the instrumentation must itself be measured — it must not significantly distort the metrics it's trying to observe") is exactly why LCP/CLS/INP's real edge cases (layout-shift session windowing, back/forward-cache restores, soft navigations) are left to the library that already gets them right, not hand-rolled. Confirmed current API (`onCLS`/`onINP`/`onLCP`/`onFCP`/`onTTFB`, `Metric` shape) directly against the installed package's own `.d.ts` files, not just the README, since `INP` replaced `FID` as a Core Web Vital at some point and training data could be stale on that
  - [x] `EventType`/`WireEventType` extended to a three-member discriminated union (`error` | `network` | `performance`) — same "extend when a real third member exists" pattern Step 4/7 already established
  - [x] Navigation timing, resource timing, and long tasks — the rest of `instrumentation.md`'s Performance list — deliberately deferred, not forgotten, same "fetch alone covers the modern case, XHR is separate work" scoping `network.ts` already used. Sampling (`instrumentation.md`: "strong candidate for configurable sampling") also deferred — no volume problem exists yet to size a sampling strategy against
  - [x] Auto-registered inside `init()` alongside error/network instrumentation, same guard against double-registration on a repeat `init()` call
  - [x] New tests: `performance.test.ts` mocks `web-vitals` itself (its algorithms are Google's to test, not this SDK's) to verify `registerPerformanceInstrumentation()` wires up all five metrics and that the adapter maps `web-vitals`' `Metric` shape to `PerformancePayload` correctly; `event.test.ts` extended for `createPerformanceEvent`. 36 sdk tests passing (was 33), run 3x consecutively to confirm the shared-mock-registry pattern (`errors.test.ts`/`network.test.ts` had to grow a third stubbed export, same lesson as before) still holds
  - [x] Go `internal/telemetry.Event`: `PerformancePayload` (`MetricName, Value, Rating, NavigationType`) added as the struct's third discriminated payload variant — `Event.Validate()` requires `payload.metric_name` but deliberately *not* a non-zero `value` (a CLS value of `0` is a real "no layout shift occurred" measurement, not a missing one — the same zero-isn't-absent nuance web-vitals itself has to handle). `ProcessEventService`/`EventWriter` needed no code changes — both already switch generically on `EventType` via `PayloadJSON()`, so a third variant just worked
  - [x] **Real end-to-end verification, not just unit tests**: ran ingestion + worker against the real local Postgres/Redpanda/ClickHouse, `curl`'d a real performance event through `/ingest/v1/events`, confirmed it landed in ClickHouse with the correct `payload` JSON via a direct HTTP query — plus a regression check that error events and genuinely-unsupported `event_type`s still behave exactly as before
  - [x] **Real bug found during that E2E pass, not a code bug — same class as Network's #16, worse this time**: found *four* separate stale `go run ./cmd/worker`/`./cmd/ingestion` child binaries left running from earlier sessions' E2E testing (Network, Session), still bound to the shared dev Redpanda topic's single partition and to port 8081, running old pre-Performance code. `kill -9` on a `go run` wrapper's PID does **not** reliably kill its compiled child process — the actual leak mechanism, confirmed by watching a *fresh* `kill` immediately produce two more orphaned children from the very next `go run` invocation. Fixed for this session with `pkill -9 -f "go-build.*worker$"` / `...ingestion$"` instead of PID-based kills; worth remembering for every future manual E2E check in this repo
  - [x] New tests: `Validate()` table cases for `PerformancePayload` (missing payload, missing `metric_name`, a zero `value` explicitly passing), `TestEvent_JSONRoundTrip`'s third case, `ProcessEventService` skips fingerprinting for performance events, `TestEventWriter_WriteEvent_PerformanceEvent` (real ClickHouse integration test), `IngestService` accepts a valid performance event — all passing, `golangci-lint run ./...` clean
  - [x] `GET /projects/:projectId/performance` (control-api) aggregates by `metric_name` — same ADR-023 GROUP-BY-over-events shape as issues/network/sessions, no materialized table. `value`/`rating` live inside the raw `payload` JSON string column (ADR-008), pulled out per-row with ClickHouse's `JSONExtractString`/`JSONExtractFloat`. p50/p75 (not p95, unlike Network's durations) matches how Web Vitals are conventionally reported — CrUX/PageSpeed both score a page on its 75th percentile. No `limit` param unlike issues/network/sessions: there are only ever five possible `metric_name` values, so `GROUP BY metric_name` can never return more rows than that regardless of event volume
  - [x] `apps/web`: `/projects/:projectId/performance` route + `PerformanceList` — same `useSuspenseQuery` pattern as the other three. CLS formatted as a unitless score (`0.050`), the other four metrics as milliseconds (`1800ms`) — formatting CLS as "0ms" would be actively misleading, not just imprecise. Four-way cross-nav completed across Issues/Network/Sessions/Performance now that all four are real
  - [x] **Real end-to-end verification, full stack**: ran ingestion + worker + control-api + the actual `vite dev` dashboard together, sent 4 real performance events (3 LCP across all three ratings, 1 CLS) through `/ingest/v1/events`, confirmed the aggregation via a direct `curl` to the new endpoint, then confirmed the identical numbers (CLS `0.050`/`0.050`, LCP `2200ms`/`3350ms` p50/p75, `33.3%`/`100.0%` good rates) rendered in the real SSR'd HTML from the running dashboard
  - [x] New tests: `listPerformanceMetrics` (groups by metric_name, keeps different metrics separate, ignores non-performance events, respects a route filter, empty-project case) against real ClickHouse; route-level tests for the new endpoint; `PerformanceList` component tests (empty state, ms-formatted row, CLS-as-unitless-score row, separate rows per metric)
  - [x] `biome check` + `tsc --noEmit` clean across all 4 TS packages. All unit + integration tests passing
- [x] Release — Postgres entity, control-api CRUD, health aggregation, and dashboard, all done
  - [x] Real gap this sub-item needed that Context/Network/Session/Performance didn't: those four are all telemetry-shaped (a new `event_type` flowing through the existing SDK -> ingestion -> ClickHouse pipeline), but Release is a control-plane concept (a deployment record), not telemetry — the SDK already sends a free-text `release` string on every event today (Step 4's `SdkConfig.release`, stored as ClickHouse's `release` column), so no SDK or Go work was needed here at all, only a new Postgres table + control-api endpoints
  - [x] New `releases` table (Drizzle schema + migration): `id, project_id, version, commit_sha, deployed_at, created_at`, unique on `(project_id, version)`, `project_id` a real enforced foreign key to `projects.id` — the first actual FK anywhere in this schema (`projects.application_id`/`environment_id` are deliberately unenforced, Step 2's nullable-FK shortcut)
  - [x] **Deviation, logged, not silent**: `data-model.md` §1 has `Release` depend on `Application` and a separate `Deployment` entity (`Release + Environment + Time`, for one release rolling out to multiple environments) — neither `Application` nor `Environment` exist as real tables yet, same gap `projects` itself already has. Collapsed `Release`+`Deployment` into one project-scoped row (`deployedAt` folded directly onto `releases`) rather than building two tables around entities that don't exist — one project already stands in for "one environment" the same way it does for every other Step 7 project-scoped endpoint
  - [x] `POST /projects/:projectId/releases` (create, `onConflictDoNothing()` + a real `409` on a duplicate `(project, version)` — not a raw Postgres constraint-violation error) + `GET /projects/:projectId/releases` (list, newest `deployedAt` first — matches `release-investigation.md`'s "Releases → select release" flow, the just-shipped release is the one most likely being investigated)
  - [x] **Real end-to-end verification, live server, not just in-process route tests**: started `control-api` for real, `curl`'d a project + two releases (one with an explicit backfilled `deployedAt`) + a duplicate-version attempt + the list endpoint, confirmed every response and the underlying Postgres rows directly via `psql`
  - [x] New tests: create with generated id, defaults (`commitSha` null, `deployedAt` now) when omitted, explicit `deployedAt` respected, duplicate `(project, version)` returns `409` not a crash, list ordered newest-first (proven via a deliberately-backfilled-old release, not insertion order), empty list for a project with none — all against real local Postgres, same integration-test pattern `projects.test.ts` already established
  - [x] `GET /projects/:projectId/releases/:version/health` (control-api): joins the Postgres `releases` row against ClickHouse events filtered to `release = version` — error count, distinct-fingerprint issue count (`uniqExactIf(fingerprint, fingerprint != '')`, "how many things broke," not "how many times"), network request/failure counts + rate, and a full performance-metrics breakdown. Reuses `listPerformanceMetrics` (extended with a new `release` filter, alongside `listNetworkResources`) rather than duplicating its quantile logic — only the error/issue and network aggregates needed new queries, since network's own `listNetworkResources` groups by `(method, resource)`, a finer breakdown than release health needs
  - [x] `apps/web`: `/projects/:projectId/releases` (`ReleaseList`) + `/releases/:releaseId` (`ReleaseHealthView`, `releaseId` a `"{projectId}:{version}"` composite — same pattern as `IssueSummary.issueId`/`SessionSummary.sessionId`). Five-way cross-nav completed across Issues/Network/Sessions/Performance/Releases now that all five are real
  - [x] **Real bug found via the live dashboard, not caught by any test**: the release detail page was first built as `/projects/:projectId/releases/:version`, nested under the existing `/projects/:projectId/releases` list route by file-based-routing convention — TanStack Router made the list route an implicit layout parent, which needs to render an `<Outlet />` for a nested child to ever appear. It didn't, so the detail URL matched successfully (confirmed in the router's own internal match data) but rendered the *list* page's content, not the detail page's — silently, no error anywhere. Found only by actually curling the running dev server and reading the returned HTML, not by any test (a `QueryClientProvider`-only component test never exercises real route nesting). Fixed by making the detail page a flat top-level route (`/releases/$releaseId`) instead, matching how `issues.$issueId.tsx`/`sessions.$sessionId.tsx` already avoid this exact trap — confirmed against TanStack Router's own docs (a `_` suffix escape hatch exists too, but the flat-route pattern already established by Issue/Session detail was the more consistent fix)
  - [x] **Real end-to-end verification, full stack**: ran ingestion + worker + control-api + the actual `vite dev` dashboard together, registered a real release via `POST`, sent one real error + network + performance event through `/ingest/v1/events` tagged with that release, confirmed `/releases/:version/health` via direct `curl`, then confirmed the identical numbers rendered in the real SSR'd HTML from the running dashboard — including the list page's link and the fixed detail page
  - [x] New tests: `getReleaseHealth` (joins Postgres + ClickHouse correctly, distinct-fingerprint issue counting, zero-network-telemetry case returns `0` not `NaN`, null for a nonexistent release) against real Postgres + ClickHouse; route-level tests for the health endpoint (200 with all-zero telemetry, 404 for an unknown version); `release` filter added to `listNetworkResources`/`listPerformanceMetrics` with new test coverage; `ReleaseList`/`ReleaseHealthView` component tests — all passing, `biome check` + `tsc --noEmit` clean across all 4 TS packages

**Milestone: Step 7 is done.** Context, Network, Session, Performance, and Release are all real, verified end to end at every layer (SDK where applicable, Go ingestion where applicable, control-api, dashboard). `mvp.md` §2's full hypothesis — correlating errors with sessions, network, performance, and releases — is now real, not just the build-gate slice Step 6 closed.

---

## Step 8 — Operations

- [x] Alerts (new issue, error spike, performance degradation, health degradation) — **all three E13-alerts.md alert types (new_issue, error_spike, performance_regression) fully done: rules, detection, notification, dashboard**
  - [x] Scope confirmed with the user before building (E13-alerts.md covers three genuinely different alert types plus a full Triggered→Acknowledged→Recovered→Resolved lifecycle — too much ambiguity to guess at, unlike Step 7's sub-items which each had one obvious shape already): **new_issue** first, **webhook URL** as the notification destination (simplest real delivery — no external service credentials needed, Slack/email can consume a webhook later). error_spike/performance_regression need real threshold/window config and are deferred to their own chunks, not forgotten
  - [x] New `alert_rules` (`id, project_id, type, webhook_url, enabled, created_at, updated_at`) + `alert_events` (`id, alert_rule_id, fingerprint, state, triggered_at, notified_at`) tables. `type`/`state` are real enums, not bare strings — `alert_event_state` defines the *full* `alert-investigation.md` state machine now (Postgres enum widening is real migration friction) even though this slice's worker will only ever write `"triggered"`; `acknowledged`/`recovered`/`resolved` transitions are deferred behavior, not deferred schema
  - [x] `alert_events` has a `unique(alert_rule_id, fingerprint)` constraint — this *is* the dedup mechanism the not-yet-built alert-worker will rely on (US-13.02: "repeated evaluations do not create uncontrolled duplicate notifications"), an atomic database guarantee via `onConflictDoNothing()` (same pattern `releases`' duplicate-version handling already uses), not a check-then-insert race
  - [x] `POST /projects/:projectId/alert-rules` (create — `format: "uri"` validation rejects a malformed webhook destination before it ever reaches a delivery attempt, satisfying US-13.01's "invalid rules cannot be saved" without needing the worker to exist first) + `GET /projects/:projectId/alert-rules` (list) + `PATCH /projects/:projectId/alert-rules/:ruleId` (enable/disable, satisfying US-13.01's "rules can be enabled or disabled" — 404s both for an unknown rule id and for a rule id that belongs to a different project, not just the first)
  - [x] **Real end-to-end verification, live server**: started `control-api` for real, `curl`'d a project + a valid rule + a rejected malformed-URL rule (422) + the list endpoint + a disable via `PATCH`, confirmed every response and the underlying Postgres row directly via `psql`
  - [x] New tests: create with generated id/defaults, malformed `webhookUrl` rejected (422), list (including empty-project case), enable/disable (including 404 for an unknown rule id and 404 for a rule belonging to a different project) — all against real local Postgres, same integration-test pattern `releases.test.ts` already established
  - [x] **Real architectural decision, written up as ADR-025, not just built where a stub happened to exist**: the detection/notification loop runs as a TypeScript entry point in `apps/control-api` (`src/alertEvaluator.ts`), *not* the Go `cmd/alert-worker` stub scaffolded early in the project. Building it in Go would've meant either violating ADR-022's "Go never writes to control-plane tables" boundary (recording a firing means writing `alert_events`) or standing up a new Bun-owned endpoint for Go to call (ADR-022's own *rejected* option, for latency reasons that don't even apply to a low-frequency poll loop) — real new infrastructure for a feature that needs none of Go's throughput characteristics, when `control-api` already has full Postgres + ClickHouse access. `cmd/alert-worker` stays an unimplemented placeholder, reserved for a future alert type that's genuinely Go-shaped
  - [x] `runAlertEvaluationCycle()`: for every enabled `new_issue` rule, finds issues with `first_seen_at >= rule.createdAt` (`db/issues.ts`'s new `listNewIssues`, reusing `ISSUE_SUMMARY_SELECT`/`toSummary` rather than a separate query shape — a rule created against a project with pre-existing issues doesn't spam-fire for all of them at once), records+dedupes via `alert_events` (`onConflictDoNothing()` against the unique constraint — an atomic "already alerted" guarantee, not a check-then-insert race), and delivers a webhook (`lib/webhook.ts`, bounded 5s timeout, no retry this slice — a failed delivery still leaves its dedup row in place, so the same issue is never re-notified even though this attempt didn't succeed)
  - [x] **Real bug found and fixed via empirical verification, not assumption**: `DateTime64(3)` query *parameters* reject ISO-8601's `T`/`Z` the same way `JSONEachRow` inserts already do (a gotcha this codebase hit once before, Step 5) — confirmed directly by writing a throwaway probe test and watching it fail with ClickHouse's own "only 23 of 24 bytes was parsed" error, not by assuming the earlier insert-side fix also covered query params. Fixed with a new shared `toClickHouseDateTime64()` helper in `db/clickhouse.ts` (the first place in this codebase that ever needed to convert a real `Date` into a query param — every other `from`/`to` filter was always an opaque string passed straight through from an HTTP caller)
  - [x] **Real end-to-end verification, the full live pipeline, not just the test suite**: ran ingestion + worker + `control-api` + a real local webhook-receiver HTTP server + the actual `alert-evaluator` process (`bun run src/alertEvaluator.ts`, 3s poll interval) together, sent one real error event through `/ingest/v1/events`, watched the evaluator's own log detect it, deliver a real webhook, and — confirmed over multiple subsequent real poll cycles — never re-fire for the same issue
  - [x] **Real leftover-data bug found while doing that verification**: an earlier version of `alertEvents.test.ts` had a foreign-key cleanup-ordering bug (deleting a rule before the `alert_events` row still referencing it) that aborted mid-`afterAll` and left orphaned rows in the local dev Postgres from that failed run — only noticed because the live evaluator run reported "evaluated 2 rule(s)" when only 1 was expected. Fixed the test (track every created rule id, delete events before rules before the project) and manually cleaned up the orphaned data
  - [x] New tests: `listNewIssues` (`since` boundary respected both ways), `listEnabledNewIssueRules` (enabled vs disabled), `recordAlertEvent`/`markAlertEventNotified` (dedup gate, cross-rule independence, notified timestamp), `deliverWebhook` (against a *real* local `Bun.serve()` server, not a mocked `fetch` — 2xx/non-2xx/unreachable-endpoint cases), and the full-cycle `runAlertEvaluationCycle` integration test (real Postgres + real ClickHouse + a real HTTP receiver, asserting both first-fire delivery and second-cycle non-repeat) — all passing, `biome check` + `tsc --noEmit` clean
  - [x] `GET /alert-rules/:ruleId` + `GET /alert-rules/:ruleId/events` (control-api): flat, not nested under `/projects/:projectId` — `ruleId` is a real Postgres uuid, already globally unique, so the dashboard's detail page doesn't need to know the project id ahead of time. `db/alertEvents.ts` gained `listAlertEvents(ruleId)`, newest-first
  - [x] `apps/web`: `/projects/:projectId/alert-rules` (`AlertRuleList` — the dashboard's **first mutation UI**, a create form + enable/disable buttons, everything before this was read-only display) + `/alert-rules/:ruleId` (`AlertRuleDetail` + `AlertEventList`, flat top-level route from the start — learned from Release's `<Outlet />` bug, no need to rediscover it). `lib/api.ts`'s `apiFetch` gained `method`/`body` support (its first non-GET use). Six-way cross-nav completed across Issues/Network/Sessions/Performance/Releases/Alerts now that all six are real
  - [x] **Real, significant bug found via this feature's own E2E verification — affects every existing dashboard page, not just Alerts**: `apps/web`'s `QueryClient` was a module-level singleton (`lib/query-client.ts`), shared across *every* SSR request within one running server process. TanStack Query's own SSR guide calls this out explicitly: creating the client at file-root scope "makes the cache shared between all requests... leaks any sensitive data." Confirmed directly, not just from the docs: created an alert rule via a real `POST`, re-fetched its list page's SSR HTML in the same `vite dev` process, and it kept rendering the pre-creation *empty* state — the singleton had cached that empty result on an earlier request and never invalidated it for a different one. This class of bug had no way to surface before Alerts: every prior dashboard feature (issues/network/sessions/performance/releases) was pure read-only display, so "create something, then immediately re-read it in the same server process" never happened until this PR's own mutation
  - [x] Fixed per the official pattern, verified against the actual installed `@tanstack/react-router-ssr-query` package source (not just the docs prose): `lib/query-client.ts`'s singleton became a `createQueryClient()` factory; `router.tsx`'s `getRouter()` (already called fresh per request by TanStack Start — that's the whole point of exporting a function instead of an instance) now creates a fresh `QueryClient` and wires it via `setupRouterSsrQueryIntegration({ router, queryClient })`, which handles dehydrate/hydrate *and* wraps the router tree in `<QueryClientProvider>` automatically; `__root.tsx`'s manual `<QueryClientProvider>` (now redundant) removed. Reproduced the exact failing sequence again after the fix — first request now correctly shows already-existing data, and a second rule created mid-session shows up on the very next request, no staleness
  - [x] New tests: `listAlertEvents` via new route-level tests (empty list, newest-first ordering); `AlertRuleList`/`AlertEventList`/`AlertRuleDetail` component tests, including the dashboard's first mutation test (`fireEvent` + `waitFor` against mocked `createAlertRule`/`setAlertRuleEnabled`, with `global.fetch` stubbed for the invalidation-triggered background refetch so it doesn't leak a real network call into the test) — all passing, run 3x consecutively, `biome check` + `tsc --noEmit` clean across all 4 TS packages
  - [x] **error_spike / performance_regression alert types (US-13.02/US-13.03)** — the deferred chunk above, now built. `alert_rule_type` enum widened (`new_issue` → `+ error_spike, + performance_regression`), plus a new `alert_metric_name` enum (the five Core Web Vitals) and four nullable condition columns on `alert_rules` (`window_minutes`, `threshold_count`, `metric_name`, `threshold_value`) — flat columns, not a JSON condition blob, matching every other table in this schema; only the columns matching a rule's `type` are ever populated, enforced by the request body, not by application-level discipline
  - [x] `POST /projects/:projectId/alert-rules` body is a real discriminated union (`t.Union` over three `t.Object` variants keyed by `type`) — an `error_spike` rule missing `thresholdCount`, or a `performance_regression` rule with an unknown `metricName`, is a 422 before it ever reaches the database, the same "invalid rules cannot be saved" guarantee `new_issue`'s `webhookUrl` validation already had
  - [x] `db/issues.ts` gained `countRecentErrors(projectId, since)` — a raw event count (not `count(DISTINCT fingerprint)`), since US-13.02's "error activity increases unexpectedly" means repeated occurrences of one recurring issue should count, not get collapsed away the way `listIssues`' GROUP BY would
  - [x] `db/performance.ts` gained `getRecentMetricWindow(projectId, metricName, since)` — a global (non-GROUP-BY) aggregate for one metric over a window: p75 via `quantile(0.75)`, plus `argMax(release/route, client_timestamp)` for US-13.03's "notifications include relevant route/release context where available"
  - [x] **New dedup mechanism for threshold-over-a-window conditions**: `new_issue`'s dedup key is a stable per-issue fingerprint, which doesn't apply here — the same spike/regression condition can legitimately still be true on the very next poll. Instead, `alertEvaluator.ts`'s `windowBucketFingerprint()` buckets time itself into non-overlapping `windowMinutes`-sized slices and fingerprints by which slice `now` falls in, so `alert_events`' existing `unique(alert_rule_id, fingerprint)` constraint still gives exactly one notification per slice with zero new schema or state tracking — and a still-ongoing incident that crosses into the next slice correctly notifies again rather than going silent after the first firing
  - [x] `runAlertEvaluationCycle(now = new Date())` — `now` is a parameter, not read fresh per rule type internally, so one cycle evaluates every rule against the exact same instant (otherwise two rules with the same `windowMinutes` could land in different buckets purely from evaluation order). `EvaluationCycleResult` gained `errorSpikesNotified`/`performanceRegressionsNotified` alongside the existing `issuesNotified`
  - [x] `apps/web`: the create form's type selector switches its condition fields per type (mirroring the API's discriminated body exactly, so a half-filled condition can't be submitted), a shared `describeAlertCondition()` renders each type's condition as one human-readable line (e.g. "≥ 25 errors in 10m", "LCP p75 ≥ 2500 over 15m") in both the list table and the detail page
  - [x] New tests: `listEnabledErrorSpikeRules`/`listEnabledPerformanceRegressionRules` (db), `countRecentErrors`/`getRecentMetricWindow` (window boundary respected both ways, release/route context via `argMax`), POST validation (both new types created successfully + both rejected for a missing/invalid condition field), and two new full-cycle `alertEvaluator.test.ts` integration tests (real Postgres + real ClickHouse + a real HTTP receiver) proving both first-fire delivery and same-bucket non-repeat for each type — plus web component tests for the new form fields and condition display. `apps/web`'s pre-existing tests needed updating too: the type `<select>`'s own `<option>` text ("new_issue" etc.) collides with the table's Type column under an unscoped `getByText`, fixed by scoping to `within(screen.getByRole("table"))`
  - [x] **Real end-to-end verification, the full live pipeline**: ran ingestion + worker + `control-api` + a real webhook receiver + the `alert-evaluator` (3s poll interval) together, created one `error_spike` rule (≥3 errors/5m) and one `performance_regression` rule (LCP p75 ≥1000ms/5m) via real HTTP through the dashboard's own routes, sent 3 real error events + 1 real poor-LCP event through `/ingest/v1/events`, and confirmed via the webhook receiver's log, `alert_events` rows in Postgres, and the dashboard's own SSR HTML (list page's condition column, detail page's fired-events table) that both fired exactly once. Left running longer than intended (background work continued in parallel) and got an unplanned bonus confirmation of the window-bucket design: enough real time passed to cross the 5-minute bucket boundary, and the still-ongoing "spike" correctly re-fired for the new bucket — dedup held within a bucket, re-fired across one, exactly as designed
- [x] Internal observability (FrontWatch monitoring itself) — **every service done**: `/health/live`, `/health/ready`, `/metrics` on ingestion, worker, control-api, and the alert-evaluator
  - [x] ADR-026: Prometheus text exposition format, `github.com/prometheus/client_golang` (Go) + `prom-client` (TS, confirmed Bun-compatible before adopting — no peer deps, pure-JS runtime deps) — no metrics library existed anywhere in the codebase yet, a real open technology choice, not an obvious default
  - [x] `internal/platform/health`: shared `Register(mux, checks)` — `/health/live` never touches a dependency (a slow Postgres must never make a live process look dead to an orchestrator's liveness probe), `/health/ready` runs every service-supplied `Checker` and reports 503 + a per-dependency `up`/`down` breakdown if any fail. `internal/platform/metrics`: shared `InstrumentHandler` records request count + duration labeled by *route pattern* and status — never the resolved path, per operations.md's high-cardinality-label caution
  - [x] `cmd/ingestion`: `/health/ready` checks Postgres (`pgxpool.Ping`) + Redpanda (new `queue.Publisher.Ping`, a thin wrapper over `kgo.Client.Ping`); `/metrics` adds `ingestion_http_requests_total`/`_duration_seconds` (by route+status) and `ingestion_events_accepted_total`/`ingestion_events_rejected_total` (by the two real rejection codes `INVALID_EVENT`/`QUEUE_PUBLISH_FAILED`) — operations.md's "telemetry ingestion" signal, made real
  - [x] `cmd/worker`: gains its **first-ever HTTP server** (previously a pure consumer loop, no listener at all) on its own port (`WORKER_METRICS_PORT`, default 8081) for `/health/live`, `/health/ready` (ClickHouse + Redpanda), and `/metrics` (`worker_records_consumed_total`/`_processed_total`/`_failed_total` by failure reason, `worker_processing_duration_seconds`) — wired into the existing graceful-shutdown sequence, not a bolt-on that skips it
  - [x] New tests: `internal/platform/health` (liveness ignores a failing checker, readiness 200 when all pass, 503 + correct per-check up/down + the real underlying error message when one fails), `internal/platform/metrics` (status code correctly labeled, including the "handler never calls WriteHeader" implicit-200 case) — `golangci-lint run ./...`, `gofmt -l .`, `go vet ./...` all clean
  - [x] **Real end-to-end verification, live services**: started ingestion + worker for real against local Postgres/ClickHouse/Redpanda, confirmed all four health checks report `up`; separately started a second ingestion instance with a deliberately wrong Postgres password and confirmed `/health/ready` correctly returned 503 with `postgres: down` (including the real driver error) while `redpanda: up` stayed correct — proving per-dependency granularity actually works, not just the happy path. Sent one real accepted + one real rejected event through `/ingest/v1/events`, confirmed `ingestion_events_accepted_total`/`_rejected_total{code="INVALID_EVENT"}` incremented exactly as expected, confirmed the worker's `worker_records_consumed_total`/`_processed_total` incremented and the event landed in ClickHouse
  - [x] **control-api**: `lib/health.ts` (same `Checker`/live-ready shape as the Go side, TypeScript-native) checks Postgres (`db.execute(sql\`select 1\`)`) + ClickHouse (`clickhouse.ping({select:true})` — deliberately not the bare `/ping` endpoint, which "does not verify credentials" per `@clickhouse/client`'s own docs). `lib/metrics.ts` instruments every route (`control_api_http_requests_total`/`_duration_seconds` by route pattern + method + status) plus `collectDefaultMetrics()` (process CPU/memory/event-loop-lag for free)
  - [x] **Real Elysia routing gotcha found and fixed**: `index.ts`'s existing `/api/v1`-prefixed sub-app meant naively `.use()`-ing health/metrics routes into it would have produced `/api/v1/health/live` — confirmed empirically (a throwaway probe script, not assumed) that a plugin merged via `.use()` inherits its parent's `prefix`. Fixed by mounting `healthRoutes`/`metricsPlugin` on the *unprefixed* root app instead, with the actual API routes merged in as a separate prefixed sub-app
  - [x] **Real Elysia lifecycle-scoping gotcha found and fixed**: a plugin's `onRequest`/`onAfterResponse` hooks are scoped to only that plugin's own routes by default, even when merged into a parent via `.use()` — confirmed empirically that hooks silently never fired for routes defined on the parent, no error, just zero recorded requests. Fixed with `.as("global")`
  - [x] **Real Elysia hook-timing gotcha found and fixed**: `onAfterResponse` turned out (confirmed empirically) to fire *after* `.handle()`'s returned promise already resolves — a race for anything scraping `/metrics` immediately after triggering traffic, own tests included. Switched to `onAfterHandle` (synchronous, part of the actual response pipeline) — which then turned out to never fire for a thrown/validation error at all, going straight to `onError` instead (confirmed empirically too), so both are wired to the same `recordRequest()` to cover every response shape
  - [x] **control-api's own alert-evaluator** (`alertEvaluator.ts`, ADR-025) gains its **first-ever HTTP server** (`alertEvaluatorMetrics.ts` + reused `lib/health.ts`) on its own port (`ALERT_EVALUATOR_METRICS_PORT`, default 9091): `/health/live`, `/health/ready` (same Postgres/ClickHouse checks), `/metrics` (`alert_evaluator_cycles_run_total`, `_rules_evaluated_total`, `_notifications_sent_total{type}`, `_cycle_duration_seconds`)
  - [x] **Real bug found and fixed**: `control-api` and the alert-evaluator both calling `prom-client`'s `collectDefaultMetrics()` against the library's shared *global default* registry seemed harmless (separate real OS processes in production) — until `bun test` loaded both modules into one shared process and the second call threw `"already been registered"`, cascading into ~136 unrelated test failures across the whole suite. Fixed correctly, not just for tests: each service now owns a dedicated `new client.Registry()` — genuinely more correct either way, since each service's metrics were always conceptually independent
  - [x] New tests: `lib/health.test.ts`, `lib/metrics.test.ts` (including a regression test for the `onAfterHandle`-doesn't-cover-errors gotcha above), `alertEvaluatorMetrics.test.ts` — all passing, stable across 13 consecutive full-suite runs (one very-low-probability transient flake seen once mid-session, never reproduced again), `biome check` + `tsc --noEmit` clean
  - [x] **Real end-to-end verification, live services**: started `control-api` for real, confirmed `/health/live`/`/health/ready` (up), confirmed `/api/v1/health/live` genuinely 404s (proving the prefix fix), sent real project/issue/malformed-alert-rule requests and confirmed `/metrics` recorded each one with the correct route pattern + status (including a 404 with no matched route safely labeled `route="undefined"`, never the raw attempted path) — then deliberately broke ClickHouse credentials and confirmed `/health/ready` correctly returned 503 with the real driver error while Postgres stayed `up`. Separately started the alert-evaluator for real, confirmed its own health/metrics on :9091, created a real `new_issue` rule + a real error event, and watched `alert_evaluator_notifications_sent_total{type="new_issue"}` increment from a real webhook delivery
  - [x] Deferred, not forgotten (ADR-026): per-query-function database latency (this slice covers HTTP-request-level latency + the two readiness pings, not a timing wrapper around every `db/*.ts` query function), distributed tracing across ingestion→queue→worker→ClickHouse, consumer-lag/queue-depth (needs a Kafka admin-client call `franz-go` doesn't do for free), and any actual dashboard/alerting built on top of these raw signals (`observability.md`'s SLI/SLO/on-call/anomaly-detection apparatus)
- [x] Backup / restore — **closes Step 8**
  - [x] Scoped to the control-plane Postgres database only (`projects`, `alert_rules`, `alert_events`, `releases`) — `infrastructure.md`'s explicit list for this slice ("back up PostgreSQL, configuration, alert definitions, identity/configuration metadata"). ClickHouse telemetry backup, object storage/source maps, and a scheduled/automated cadence are explicitly deferred, tracked in the new runbook, not silently skipped — `infrastructure.md` itself notes telemetry backup policy is a separate, not-yet-made decision ("depends on retention/durability requirements")
  - [x] `scripts/backup-postgres.sh` (`pg_dump -Fc`) / `scripts/restore-postgres.sh <dump> [target]` / `scripts/verify-postgres-backup.sh [dump]` — the last one runs `infrastructure.md`'s full validation sequence in one command: backup (if none given) → restore into an isolated `frontwatch_restore_test` database → per-table row-count integrity check. Also runnable via `bun run backup:postgres`/`restore:postgres`/`verify:postgres-backup`
  - [x] **Restoring over a live database is a real, deliberate, separate action, not this script's default**: `restore-postgres.sh` refuses any target whose name doesn't contain `_restore_test` unless `ALLOW_UNSAFE_RESTORE_TARGET=1` is set explicitly — confirmed the refusal actually fires against a real attempt to restore into the live `frontwatch` database
  - [x] `docs/08-operations-release/runbooks/postgres-backup-restore.md` — first runbook in the repo (`observability.md` names "backup failed" explicitly in its own runbook list). Covers the scripts, why restore-testing is a real requirement here (not a formality — a `pg_dump` that completes without error still doesn't prove the backup is usable), the real-recovery procedure (declare recovery mode, stop `control-api`/the alert-evaluator first since both write to this database, confirm the RPO window, restore, validate via `/health/ready`), and explicitly notes formal RPO/RTO targets aren't defined yet (`infrastructure.md` wants these "per customer deployment tier," which doesn't exist pre-pilot)
  - [x] **Real bug found and fixed**: Homebrew's default `pg_dump`/`pg_restore` (v17) embeds a `SET transaction_timeout = 0;` preamble that this repo's `docker-compose` Postgres (v16) doesn't understand — `pg_restore` failed with `unrecognized configuration parameter "transaction_timeout"`, a genuine client/server version mismatch, not a bug in the scripts' own logic. A dump taken with the mismatched v17 tools also turned out to be unrestorable by v16 tools at all (`unsupported version (1.16) in file header`) — confirmed by regenerating the dump with version-matched tools and having it restore cleanly. Fixed by adding a `PG_BIN_DIR` override (all three scripts) so version-matched client tools can be pointed at explicitly, documented in both the scripts' own comments and the runbook, not just worked around locally once
  - [x] `shellcheck` clean on all three scripts (installed for this — not previously part of this repo's toolchain, but this is the point where the repo gained its first real shell scripts)
  - [x] **Real end-to-end verification, live services**: seeded a real project + alert rule + release into the local dev Postgres, ran the full `verify-postgres-backup.sh` sequence against real version-matched tooling, confirmed all four tables' row counts matched exactly between source and restored (`1`/`1`/`1`/`0`, not `0`/`0`/`0`/`0` — a genuinely populated comparison, not an accidentally-trivial one), confirmed the safety guard actually refuses a real attempt to restore into the live database name, cleaned up all test data/databases afterward

**Milestone: Step 8 is done.** Alerts (all three types), internal observability (every service), and backup/restore are all real and verified. Remaining Step 9 items (auth/RBAC, tenant isolation, load testing, failure recovery, disaster recovery testing) and Step 10 (pilot) are next.

---

## Step 9 — Hardening

- [x] Full auth / RBAC (Administrator / Engineer / Viewer) — **identity, sessions, and RBAC enforcement all done**: every project-scoped endpoint now requires a real authenticated principal with sufficient role
  - [x] Scope confirmed with the user before building (two real, hard-to-reverse decisions — tech-stack.md itself flags the auth mechanism as a "possible ADR," same ambiguity class as Step 8's Alert type/notification scope): **local email+password** (not Keycloak/OIDC yet) as the mechanism, and **split into identity-first-then-enforcement PRs** rather than one large PR
  - [x] ADR-027: local email+password with server-side sessions (`Bun.password`, argon2id — verified via a real hash/verify round-trip in a throwaway probe before adopting it, not assumed). OIDC/Keycloak stays the documented eventual direction (tech-stack.md), deferred until a real enterprise SSO need exists (strategy.md scopes that to a later product stage)
  - [x] `organizations` / `users` / `memberships` (carrying `role`: administrator/engineer/viewer) / `auth_sessions` tables — data-model.md §1's Organization/User/Membership model, built for real. One status enum per entity (not shared), matching `projectStatus`'s own precedent. `auth_sessions.token_hash` stores a SHA-256 hash of the session token, never the raw value — same reasoning `passwordHash` is hashed, just a fast hash since a session token is already high-entropy random, not a human-chosen secret
  - [x] `db/registration.ts`: `registerOrganizationAndAdmin()` — one atomic `db.transaction()` creating the organization + user + administrator membership together (US-01.01: "creating an org makes the creator an Administrator"), so a crash mid-registration or a duplicate email can never leave an orphaned organization with no admin. Duplicate email uses the same `onConflictDoNothing()` + typed-error pattern `releases.ts`'s duplicate-version handling already established, not a raw constraint-violation catch
  - [x] `routes/auth.ts`: `POST /auth/register`, `POST /auth/login`, `POST /auth/logout`, `GET /auth/me` — login returns the identical `401 "invalid email or password"` whether the email doesn't exist, the password is wrong, or the account is disabled (no user enumeration), and runs `verifyPassword` against a real dummy hash on the no-such-user path so a nonexistent email doesn't respond measurably faster (the same timing-safety shape ingestion's own invalid-vs-inactive credential response already used)
  - [x] Session cookie: `httpOnly`, `sameSite: "strict"`, `secure` conditional on `NODE_ENV=production` (a hardcoded `secure: true` would silently break login over the local dev stack's plain http)
  - [x] `lib/sessionPrincipal.ts`'s `resolveSessionPrincipal()` — the one function that turns a raw cookie token into an `AuthenticatedPrincipal`, used by `/auth/me` now and reused as-is by the RBAC-enforcement PR's middleware next, rather than reimplementing session lookup twice
  - [x] New tests: `lib/auth.test.ts` (password round-trip, token uniqueness/determinism, expiry math), `db/registration.test.ts` (atomic creation, duplicate-email rollback — confirms the orphaned-org failure mode specifically doesn't happen), `db/authSessions.test.ts`, `db/memberships.test.ts` (disabled memberships never surface), `routes/auth.test.ts` (full register→login→me→logout→me-fails-again lifecycle through real HTTP, replaying a real `Set-Cookie` as a real `Cookie` header the way a browser would) — all passing, stable across 3 consecutive full-suite runs, `biome check` + `tsc --noEmit` clean
  - [x] **Real end-to-end verification, live server**: started `control-api` for real, ran the complete lifecycle through actual HTTP with a real cookie jar (`curl -c`/`-b`) — register → `/auth/me` → logout → `/auth/me` correctly 401s afterward; separate login → `/auth/me` succeeds; wrong password and unregistered-email both 401 with the identical message; duplicate email 409; weak password 422. Confirmed directly via `psql` that `users.password_hash` is a real argon2id hash (not plaintext) and `auth_sessions.token_hash` is a SHA-256 hash that does not match the raw cookie value the client actually holds
  - [x] **RBAC enforcement — the deliberately separate second PR**, confirmed with the user before starting it. `projects.organization_id` is now a real, `NOT NULL`, FK-enforced column (data-model.md §1's actual tenant boundary, not just the `project_id` scoping every route already had) — the first schema change in this codebase that widened an existing table's *required* columns, not just an addition
  - [x] `lib/authorization.ts`: `authorizeProjectAccess(principal, projectId, minRole)` / `authorizeOrganizationAccess(principal, organizationId, minRole)` — one shared function every one of the 14 existing endpoints calls, instead of each reimplementing the same three checks (security-architecture.md §6: "authorization resolves principal → organization membership → project scope → requested action"). A real, documented role-permission decision this codebase's own docs never enumerated: Viewer read-only, Engineer covers day-to-day writes (create/update projects, releases, alert rules), Administrator reserved for org/member management (not built yet)
  - [x] **404, not 403, for tenant isolation** — a project (or alert rule) a cross-org principal has no membership path to is indistinguishable from one that doesn't exist at all; 403 is reserved for a real same-organization member without enough role. Matches the precedent `alertRules.ts`'s PATCH already set for "belongs to a different project," now applied uniformly
  - [x] `lib/authPlugin.ts`: resolves `principal` once per request via a named, `.as("global")`-scoped `derive()` — **two more real Elysia gotchas found and fixed**, both confirmed empirically before trusting the fix: (1) a derive's TypeScript typing only propagates within the instance that calls `.use()` on it directly, even though `.as("global")` already makes it work at runtime — every route file calls `.use(authPlugin())` itself just for its own typing; (2) without giving the plugin instance a `{ name }`, Elysia would re-run the derive (and its session lookup) once per route file merged into a single request instead of once total — fixed by naming the instance, which makes Elysia's plugin deduplication treat every `.use(authPlugin())` call as the same plugin, confirmed via a throwaway probe counting actual invocations
  - [x] Every one of the 14 existing endpoints updated: GET routes require `viewer` (any active member), POST/PATCH require `engineer` or higher. The two flat alert-rule routes (`/alert-rules/:ruleId`, `/alert-rules/:ruleId/events`, no `projectId` in the URL) look the rule up first to learn its `projectId`, then authorize — same 404-for-both-"doesn't exist"-and-"can't see it" shape as everything else
  - [x] `routes/projects.ts`'s `POST /projects` now requires `organizationId` in the body and `engineer`-or-higher membership in it — a project can no longer be created without a real owning organization
  - [x] New `testHelpers/auth.ts`: `registerTestPrincipal()` (through the real `/auth/register` route, not a shortcut insert), `seedTestProject()`/`seedTestOrganization()`, `setTestMembershipRole()` (the only way to test Viewer/Engineer, since registration always creates an Administrator and a real invitation flow doesn't exist yet), and `cleanupTestPrincipal()` — baking in the correct `auth_sessions` → `memberships` → `users` → `organizations` deletion order after hitting that exact FK-ordering bug (a repeat of the same class this session already hit once in `db/alertEvents.test.ts`) while writing the very first updated test file
  - [x] Every existing route/db test file that seeded a project updated for the new required `organizationId` (and, for route tests, a real session cookie): `issues`, `network`, `sessions`, `performance`, `releases`, `alertRules` (routes + db), `alertEvents`, `releaseHealth`, `alertEvaluator`, `projects` — all passing, stable across 3 consecutive full-suite runs (148 tests), `biome check` + `tsc --noEmit` clean
  - [x] New `routes/tenantIsolation.test.ts` — security-architecture.md §6's explicit requirement ("automated tests for org_A → org_B access must fail") made real: one cross-tenant attempt per resource type (project, issues, network, sessions, performance, releases — both read and the release/alert-rule write paths, both flat alert-rule routes, and `POST /projects` claiming a foreign `organizationId`), all asserting 404 (403 only for the one case where the principal is real but simply not a member of the org they named) — 13 tests, all passing
  - [x] **Real end-to-end verification, live server**: started `control-api` for real, registered two real organizations (A and B) with real projects/releases/alert-rules, then ran every cross-tenant probe from Org B's real session against Org A's real project over actual HTTP — every read 404'd, every write 404'd (and confirmed via `psql` that nothing was actually written), `POST /projects` claiming Org A's id correctly 403'd, an unauthenticated request correctly 401'd — then confirmed Org A's own principal could still read/write its own project normally, proving enforcement doesn't just block everything indiscriminately
  - [x] Deferred, not forgotten: invitation flow (an Administrator adding a second user to an *existing* organization — registration only creates a brand-new org today), password reset/change, session rotation (auth.md names it explicitly), an expired-session cleanup sweep (lazy-checked on read today, never actively deleted), audit logging (data-model.md's `AuditRecord` entity), dashboard login UI (apps/web has no login screen yet — every protected endpoint is real, but nothing in the browser can reach them yet without a hand-crafted cookie)
- [x] Tenant isolation — **closed by the RBAC-enforcement work above**: `routes/tenantIsolation.test.ts` + every cross-tenant check baked into `lib/authorization.ts` *is* this checklist item, not a separate pass — see Full auth/RBAC's own entries for the real tests and the live-server verification
- [x] Load testing — `operations.md`'s Load spec ("ingestion throughput, processing throughput, query latency, queue lag, storage insert performance") made runnable against the real local stack, not simulated
  - [x] Tool choice confirmed via ADR-028: **k6** — scriptable staged-VU ramps map directly onto `test-strategy.md`'s named traffic patterns (normal → peak → incident spike → sustained high volume → recovery), built-in percentile/threshold reporting, no shipped dependency (dev tool only, `brew install k6`). A custom Go generator and vegeta/hey/wrk were considered and rejected — see the ADR for why
  - [x] `loadtest/` — `provision.sh` (real org/admin/project through the actual HTTP routes, not shortcut inserts — RBAC enforcement means a fake project/key would just measure how fast the platform rejects load), three k6 scenarios (`ingestion.js`'s staged traffic-pattern ramp, `query.js`'s ramping query concurrency, `sustained-ingest.js`'s flat background write load), `run.sh` orchestrating a full pass end-to-end, `teardown.sh` cleaning up after itself (even on failure, via `trap cleanup EXIT`) — not wired into CI, same operational-tooling status as `scripts/backup-postgres.sh`
  - [x] **Real finding — cmd/worker's processing throughput is the system's actual bottleneck, not ingestion.** Ingestion (`POST /ingest/v1/events`) sails through every traffic stage: 202,473 events accepted across a 150-peak-VU run (0 rejected, 0 failed, p95=5.93ms; a lighter 45-peak-VU re-run: 48,358 accepted, p95=7.41ms) — the untrusted-input boundary just validates and durably enqueues (ADR-003/004), so it barely notices load. `cmd/worker`, by design, processes one Kafka record at a time (one ClickHouse insert + one commit per event, so a persist failure never silently skips a record — see its own comment) — measured via `worker_processing_duration_seconds`: 512.46s / 114,372 records ≈ 4.48ms/record ≈ **~220-250 events/sec steady-state ceiling**, versus ingestion's demonstrated 385-1263 events/sec. A real spike outruns processing every time: queue lag hit 157,620 after the 150-VU run's spike stage (202,587 events published, only 44,967 consumed at that instant) and took over 15 minutes to fully drain. This is exactly the failure mode `test-strategy.md`'s own success criteria warns about ("no unbounded queue growth") — not a bug in this PR's scope to fix (batched consumption + batched ClickHouse inserts is a real, separate architecture change touching commit-offset semantics), but a genuine capacity finding surfaced before Step 10 (Pilot), not after. Logged under Deferred below; the user was asked whether to fix it now or schedule it separately
  - [x] **Real bug found and fixed while building the harness**: `run.sh`'s first version tore down the test project (deleting its Postgres row and its ClickHouse events) after only a bounded 30s wait for the queue to drain — with the worker still ~150k events behind at that point, it kept processing and re-inserting rows under the now-deleted `project_id` for several more minutes *after* "teardown complete" had already printed, silently resurrecting deleted data as orphaned rows. Caught by checking `rpk group describe` after the run instead of assuming the 30s grace period was enough. Fixed two ways: `run.sh` now polls `TOTAL-LAG` to a real zero (10-minute timeout, loud warning if exceeded) before teardown runs at all, and the scenario's default VU targets were reduced (150→45 peak) so a routine run's drain stays in the tens of seconds, not tens of minutes — the orphaned rows from the original run were manually located and purged from local ClickHouse
  - [x] **Real correctness proof, not assumed**: after the fix, total k6-reported accepted events (48,358 from the traffic-pattern run + 4,434 from the concurrent background-write scenario = 52,792) matched the ClickHouse row count for that project **exactly** — 52,792 — confirming zero event loss and zero duplication under a full traffic-pattern + concurrent-query-load run, end to end through Postgres credential validation → Redpanda → worker → ClickHouse
  - [x] Query latency (`GET /projects/:projectId/{issues,network,sessions,performance,releases}`) under ramping concurrent VUs *while* a background write load hits ingestion concurrently (`test-strategy.md`'s own reasoning: monitoring tends to spike exactly when the system it watches is failing): p90=53.85ms, p95=77.74ms (well under the 300ms threshold) — but a real tail-latency spike (max ≈30.5s, reproduced in both runs) appears, correlated with the worker's catch-up burst against ClickHouse competing with reads for the same resource. A small number of severe outliers, not a systemic slowdown (median stayed 18.6ms) — logged under Deferred below, not chased further in this pass
  - [x] **Follow-up, same day: fixed the worker's processing-throughput ceiling itself**, at the user's explicit choice ("fix worker batching now" over moving straight to Failure recovery). `cmd/worker/main.go`'s `processFetch`/`decodeRecord` now decode and validate every record from a single `PollFetches` call, then persist everything that survived as **one** ClickHouse batch (`EventWriter.WriteBatch`, the native `PrepareBatch`/`Append`/`Send` protocol) and commit through the fetch's last offset only if that batch succeeds — replacing the old one-record-in/one-INSERT-out/one-commit-out loop. Deliberately batches by "whatever one `PollFetches` call returned" rather than a custom timer/size accumulator across polls: franz-go already adapts fetch size to available data, so light traffic yields small batches (low latency) and heavy traffic yields large ones (high throughput) with no extra buffering/goroutine/timer to get wrong. All-or-nothing per batch preserves the exact same "never commit past an unpersisted record" guarantee the old code had — WriteBatch's own comment covers why a bigger blast radius on a rare persist failure isn't a new correctness risk (`ReplacingMergeTree` already de-duplicates redelivered rows by `(project_id, event_type, client_timestamp, event_id)` on merge)
  - [x] **Re-ran the identical load-test scenario after the fix — proven, not assumed**: queue lag immediately after the spike stage dropped from 18,743 (pre-fix run) to **0** (post-fix run) — the worker now fully keeps pace with ingestion through the spike, not just eventually catching up afterward. `worker_batch_persist_duration_seconds`/`worker_batch_size` (replacing the old per-record `worker_processing_duration_seconds`, now genuinely misleading once persistence is batched) measured 52,895 events persisted in 37.01s of actual ClickHouse write time ≈ **1,429 events/sec** — roughly **6x** the pre-fix ~220-250 events/sec ceiling, comfortably above this run's own ingestion rate (386/s), which is exactly why lag stayed at zero throughout. Processing correctness held again: 52,895 accepted = 52,895 ClickHouse rows, exactly. Query p95 also improved (77.74ms → 23.81ms) as a side effect of the worker no longer competing with reads for ClickHouse during catch-up — though the occasional severe tail outlier (max ≈31.7s) persisted even with zero backlog this run, so it isn't purely a catch-up-contention artifact after all; still logged under Deferred, not chased further
  - [x] New tests: `cmd/worker/main_test.go` (`decodeRecord`'s three outcomes — success, malformed JSON, validation failure — plus an empty-fetch no-op guard for `processFetch`), `EventWriter.WriteBatch`/`WriteBatch` empty-batch integration tests (real ClickHouse, three mixed event types in one batch). `go test ./...`, `golangci-lint run ./...`, `gofmt -l .`, `go vet ./...` all clean
  - [x] Deferred, not forgotten: the occasional severe query-tail-latency spike (now confirmed not solely a write-catch-up artifact, see above), resource-usage capture (CPU/memory per service during a run), a real staging/production-topology capacity-planning pass (every number above is single-laptop, all-services-colocated — see `loadtest/README.md`'s own caveat), bounded-concurrency processing (multiple fetches/batches in flight at once — still one fetch at a time), and SDK-side/browser-origin load (this harness drives the ingestion HTTP API directly, never a real browser running the SDK)
- [x] Failure recovery — `legacy-docs/17-testing-and-quality/failure-testing.md`'s principle made runnable: *"Dependencies fail. Test what happens when Postgres/ClickHouse/Redpanda become unavailable — fail gracefully, expose health status, retry only where safe, avoid retry storms, preserve durable data where possible, recover automatically where designed."* Started with a research pass (`Explore` agent auditing every failure path's *actual* code today, not what the docs said should exist) — found four real gaps, confirmed the user wanted all four fixed with a real chaos harness proving each one (not just fixed and trusted)
  - [x] **Real gap #1 — ingestion misclassified a Postgres outage as a bad credential.** `internal/telemetry/service.go`'s `Ingest()` collapsed *any* error from `ValidateCredential` (including a raw Postgres connection failure) into `ErrInvalidCredential`, which `cmd/ingestion`'s handler mapped to 401 — indistinguishable from a genuinely wrong public key. That mattered beyond the wrong status code: `packages/sdk`'s transport deliberately never retries a 401 (retrying a bad key forever would be pointless), so a real Postgres outage caused the SDK to silently **drop events instead of retrying them**, exactly the "retry only where safe" principle inverted. Fixed with a new `telemetry.ErrDependencyUnavailable` sentinel — `internal/storage/postgres`'s `ValidateCredential` (the adapter) now translates its own `ErrProjectNotFound` into `telemetry.ErrInvalidCredential` explicitly and passes any other error through untouched, so `service.go` can tell "the key is wrong" apart from "the check itself failed"; the ingestion handler now maps the latter to 503. Also added `credentialCheckTimeout` (3s) around the credential check itself — defense against a true network black hole, not just a fast connection-refused, which is all a locally-stopped container can actually reproduce
  - [x] **Real gap #2 — ingestion had no bound anywhere on the Redpanda publish path**, and this one took real digging to close completely. `internal/queue.Publisher` had no `RecordDeliveryTimeout` (franz-go's own default there is "effectively unbounded" retries) and `cmd/ingestion`'s `http.Server` had no `ReadTimeout`/`WriteTimeout`/`IdleTimeout` — confirmed empirically that killing Redpanda mid-request left a real HTTP request hanging past a 20-second client-side cutoff with no sign of returning. First fix pass added `kgo.RecordDeliveryTimeout(10s)`, a per-request `context.WithTimeout` (`requestProcessingTimeout`, 15s) wrapping the whole `Ingest()` call, and `http.Server` Read/Write/IdleTimeout as a last-resort backstop — bounded the common case (repeatedly verified at 10-16s) but **intermittently still hung past 40+ seconds** when the same long-lived ingestion process weathered several Redpanda outages back-to-back (chaostest's own scenario sequence reproduced this reliably; isolated one-off tests mostly didn't, which delayed diagnosis). Investigated as a real regression, not dismissed as flaky, in two stages: (1) found franz-go's generic *request* retries (metadata refreshes) have their own separate, undocumented-by-default 30s bound distinct from `RecordDeliveryTimeout` (which only covers *record* delivery) — added `kgo.RetryTimeout(10s)` explicitly rather than trusting the implicit default; this alone wasn't sufficient. (2) Confirmed via `bash -x` with wall-clock tracing that `ProduceSync` could still block **past its own context's deadline** — i.e. context cancellation wasn't reliably reaching every internal retry path. Fixed with a hard backstop: `PublishEvent` now runs `ProduceSync` in its own goroutine and races it against `ctx.Done()` via `select`, so the function — and the HTTP request waiting on it — always returns within the caller's deadline regardless of what franz-go is doing internally; the abandoned goroutine is still bounded by `RecordDeliveryTimeout`/`RetryTimeout` and exits on its own once nobody's listening. Proven with a deterministic (non-flaky) unit test pointing the client at a non-routable address and asserting `PublishEvent` returns at the context deadline, not later — plus two full clean runs of chaostest's own back-to-back scenario sequence after the fix (zero flakes across both)
  - [x] **Real gap #3 — the most severe one: `cmd/worker`'s ClickHouse persist-failure path could lose data permanently without a restart.** Before this fix, a failed `WriteBatch` just logged `CRITICAL` and moved to the next `PollFetches` call, leaving that batch's offsets uncommitted — but with no retry of its own, franz-go's internal fetch cursor had already advanced, so those events were only ever redelivered by an incidental consumer-group rebalance or a manual process restart, neither a real recovery mechanism. Confirmed empirically: sent 5 events during a real ClickHouse outage, restarted ClickHouse, waited — `TOTAL-LAG` stayed at 5 and 0 rows ever landed until the worker process itself was restarted. Fixed with `persistBatchWithRetry`: the *same* batch is retried with capped exponential backoff (500ms→30s) until it succeeds or the worker shuts down (`ctx` cancellation) — never abandoned. Safe to retry indefinitely specifically because `ReplacingMergeTree` already de-duplicates redelivered rows on merge (`0001_create_events_table.up.sql`), and blocking that one partition's consumption while retrying is exactly `system-architecture.md`'s own backpressure principle ("when downstream capacity is constrained, the queue grows and workers catch up") applied to a single stuck batch. Re-tested the identical scenario after the fix: worker retried 7 times (500ms→...→16s backoff), self-recovered the instant ClickHouse came back — all 5 events landed, zero restarts. `writer *chstorage.EventWriter` became a consumer-side `batchWriter` interface (matching `CredentialValidator`/`Publisher`'s existing convention) specifically so this retry/backoff logic could be unit-tested against a fake that fails on command — `TestPersistBatchWithRetry_RecoversAfterTransientFailures` and `TestPersistBatchWithRetry_StopsOnShutdown` cover both halves (self-heals; stays bounded by shutdown, not unbounded). New metrics: `worker_batch_retries_total`, and `worker_processing_duration_seconds` (per-record, no longer meaningful) replaced by `worker_batch_persist_duration_seconds` + `worker_batch_size`
  - [x] **Real gap #4 — control-api had no global error handler at all.** A stopped Postgres produced Elysia's raw default response for an uncaught exception — confirmed empirically it included the **literal failed SQL query and its bound parameters** in the response body, a direct violation of `operations.md`'s "internal errors never expose stack traces, SQL, internal topology, or secrets to API clients." Fixed with `lib/errorHandling.ts`'s `errorHandlingPlugin()`: every uncaught exception maps to a clean `503 DEPENDENCY_UNAVAILABLE`, with the real error still logged server-side (`console.error`, not silently dropped) — Elysia's own recognized categories (`VALIDATION`/`NOT_FOUND`/`PARSE`) pass through untouched. **A fourth real Elysia gotcha found and precisely characterized this session** (distinct from the three `lib/metrics.ts` already documents): `onError` only covers routes registered *after* it in the same instance's `.use()` chain — confirmed via four throwaway probes that this is true *regardless* of `.as("global")` (the opposite of `derive()`'s rule, where `.as("global")` alone is sufficient regardless of order). `errorHandlingPlugin()` is therefore `.use()`d first in `index.ts`, before `healthRoutes`/`api`. Every uncaught exception maps to the same 503 today — not a general internal-vs-dependency classifier, but an honest reflection of this codebase's current reality (a real audit found every route is a thin wrapper over a Postgres/ClickHouse call, nothing else throws) — revisit if that ever stops being true. 4 new tests in `errorHandling.test.ts`, including one proving the registration-order requirement is real (a route merged in *before* the plugin is deliberately NOT covered)
  - [x] `packages/sdk`'s ADR-006 fail-open guarantee had real test coverage for "fetch throws once then recovers" and "status persists at 503," but never the case that matters most: FrontWatch genuinely unreachable for the *entire* retry budget. Added `"resolves cleanly (never throws) when every attempt is a network failure"` to `transport.test.ts` — closes the gap `legacy-docs/17-testing-and-quality/failure-testing.md`'s own SDK section names explicitly ("a monitoring outage must not become an application outage")
  - [x] `chaostest/` — a real, repeatable chaos-testing harness, same shape and operational-tooling status as `loadtest/` (not CI-wired; stops/restarts real Docker containers). `toggle.sh` (stop/start one dependency, wait for its healthcheck to actually settle — found the hard way that a stopped container reports `unhealthy`, not absent, via `.State.Health.Status`, so "down" has to be read from `.State.Status` instead), five scenario scripts (one per real gap above, each with a `trap cleanup EXIT` restoring the dependency even if the scenario itself fails partway — a real bug hit once while building this: an earlier version without the trap left Postgres stopped after one scenario crashed, cascading into failures for every later scenario and the final teardown), `run-all.sh` orchestrating all five with a pass/fail summary. Reuses `loadtest/provision.sh`/`teardown.sh` directly rather than a second implementation of the same real-org/project setup
  - [x] Deferred, not forgotten: object storage / DNS failure / network partition / pod-killed / node-lost / certificate-problem scenarios from `failure-testing.md`'s full list (not applicable yet — no object storage integration, no Kubernetes deployment until Step 10), dead-lettering permanently-invalid records (decode/validation failures still just log and skip, unchanged by this pass), the alert-evaluator's webhook delivery having no retry policy (`lib/webhook.ts` already documents this as separately deferred), distinguishing "internal bug" (500) from "dependency unavailable" (503) in control-api's error handler once routes have real logic beyond DB calls
- [x] Disaster recovery testing — `infrastructure.md`'s *"run recovery exercises periodically, not only when something has already gone wrong"* made real, going further than Step 8's `verify-postgres-backup.sh` (which restores into an isolated `_restore_test` database and only compares row counts). Confirmed scope with the user before running anything destructive: a full exercise against the real local dev Postgres, not an isolated-only check
  - [x] `scripts/dr-exercise.sh` — seeds real baseline data through the real HTTP routes (org/project/alert rule), takes a real backup (the RPO boundary), creates *more* real data deliberately after the backup, then **actually destroys the real local Postgres container and its Docker volume** — a genuine "storage failure"/"cluster loss" scenario, one of `infrastructure.md`'s own named DR scenarios, not a database-level drop into a throwaway target. Executes the runbook's documented recovery sequence for real: restore infrastructure (recreate the container from scratch) → restore control data (`restore-postgres.sh`, `ALLOW_UNSAFE_RESTORE_TARGET=1`) → start services → validate. Reports measured RTO and the concrete RPO gap, then cleans up its own seed data
  - [x] Validation deliberately proves the RPO gap is real, not just that *something* survived: asserts the *post*-backup project is genuinely gone (404) after recovery, not merely that the pre-backup project came back. Also validates the full pipeline works again, not just Postgres in isolation — a fresh event through ingestion after recovery must reach ClickHouse via the worker, proving credential validation (Postgres-dependent) and the whole ingest→queue→worker→storage chain all recovered together
  - [x] **Real run, first attempt, 2026-08-15: 8/8 validations passed, RTO = 46 seconds.** Every health check up, pre-backup project and alert rule queryable, post-backup project confirmed absent, new telemetry ingested and landed in ClickHouse — all after the real container+volume were destroyed and recovered from a `pg_dump`/`pg_restore` cycle alone. No gaps found in the recovery sequence itself on this run
  - [x] `PG_BIN_DIR` version-matching (Homebrew pg_dump/pg_restore v17 vs. the docker-compose Postgres v16 server — the same real mismatch Step 8 already found and fixed) applies here too; the script accepts the same override
  - [x] ClickHouse stayed untouched throughout, as expected — telemetry backup remains a documented, deliberate, separate policy decision this project hasn't made yet (Step 8's own deferral, unchanged). This exercise proves the *pipeline that depends on* Postgres recovers, not that telemetry itself needs to survive a Postgres-only disaster (it was never at risk from one)
  - [x] `docs/08-operations-release/runbooks/postgres-backup-restore.md` updated with the exercise procedure and this run's real results — the runbook's own "document the incident" discipline applied to a deliberate exercise, not just a real incident
  - [x] Deferred, not forgotten: formal RPO/RTO *targets* (vs. what was *measured* here) — `infrastructure.md` scopes these "per customer deployment tier," which doesn't exist yet pre-pilot; a scheduled/repeatable exercise cadence (this is a manually-run script today, same status as the backup scripts it builds on); the same object-storage/ClickHouse-telemetry gaps Step 8 already deferred, unchanged by this pass

---

## Step 10 — Pilot

- [ ] Real, controlled customer application onboarded — genuinely blocked on a real external customer relationship, not engineering. Confirmed with the user rather than assumed: this needs a real business step (finding and onboarding a pilot customer per `roadmap.md` §3's own selection criteria) this repo's own work can't manufacture. Everything *buildable* about readiness for that moment is the item below
- [x] **`apps/demo`** — the user ran the self-hosted stack for real (registered, created a project) and hit the obvious next wall: no actual customer app exists yet to instrument with the SDK and generate real telemetry from. A small real, clickable Vite page (`@frontwatch/sdk` resolved straight from its TypeScript source via the bun workspace — the same "consume from a local path" story `self-hosted-installation.md` already documents, made concrete) with buttons that trigger genuine browser events the SDK's automatic instrumentation actually captures: an uncaught `SyntaxError` (`window.onerror` path), a manually-caught handled error (`captureException`), successful/404/slow `fetch` calls (network instrumentation), and ambient `web-vitals` performance capture from real page interaction. Not a product surface — purely so the SDK↔ingestion↔worker↔ClickHouse↔dashboard loop can be exercised by a real human clicking a real page before an actual pilot customer exists
- [x] Pilot readiness checklist satisfied (`docs/02-product/roadmap.md` §3) — **as far as engineering alone can take it.** Confirmed scope with the user first: nothing in this repo packaged FrontWatch for installation at all before this pass (no Dockerfiles, no deployment compose, no migration automation — every prior step ran via `go run`/`bun --watch` against dev infra only). Built real deployability, then dry-ran every checklist item for real against a truly fresh install — not assumed, not simulated
  - [x] **Dockerfiles for all 4 services** (`apps/control-api/Dockerfile`, `apps/web/Dockerfile`, `services/data-plane/cmd/{ingestion,worker}/Dockerfile`) — none existed before this pass. Each built and run for real, individually, before ever being wired into a compose profile. Found and fixed three real bugs along the way, none of them hypothetical:
    - bun workspaces don't fully hoist dependencies to the root `node_modules` — `apps/web`'s production server bundle needs its own per-workspace `node_modules` (react, vite, @tanstack/*) copied into the runtime image too, confirmed via a literal `Cannot find package 'react'` failure when it wasn't
    - `apps/web`'s build needs `packages/config`'s actual files (its `tsconfig.base.json`), not just its `package.json` — the lockfile-consistency install step already needed the latter for every workspace member, but the build step needed the former specifically for the one package this app's `tsconfig.json` extends
    - bun's workspace-wide lockfile refuses `--frozen-lockfile` unless *every* workspace member's `package.json` is present at install time, not just the ones a given service directly depends on
  - [x] **`infra/self-hosted/docker-compose.yml`** — Deployment Profile B ("Small self-hosted, Single VM," `infrastructure.md`'s own profile table), the single-VM variant: one compose stack containing both FrontWatch's services and their infra dependencies, distinct from `infra/local/`'s dev-infra-only profile. Postgres/ClickHouse deliberately not exposed to the host (`infrastructure.md`: "only Web/API and Ingestion are normally exposed"). Found and fixed a real bug here too: `control-api/Dockerfile`'s `migrate` stage was physically the *last* `FROM` block in the file, making it Docker's implicit default build target — both a bare `docker build` (no `--target`) *and* the compose `control-api` service (which also doesn't set `target:`, correctly expecting the implicit default to be the app) silently built the migration image instead, so the "control-api" container ran `drizzle-kit migrate` and exited instead of starting the server. Fixed by making `runtime` the last stage instead of relying on every caller remembering `--target runtime` explicitly
  - [x] **Migration automation** — neither Postgres (`drizzle-kit`) nor ClickHouse (`golang-migrate`) migrations ran automatically anywhere before this (CI was the only place either ran, as a manual step in the workflow). Two new one-shot init images (`control-api/Dockerfile`'s `migrate` target; `services/data-plane/migrations/clickhouse/Dockerfile`, wrapping the identical `golang-migrate` CLI CI already uses) run via `depends_on: condition: service_completed_successfully` before any app service starts — a fresh install's schema is real from `docker compose up`, not a manual step an operator has to remember. Verified against genuinely empty fresh Postgres/ClickHouse instances (not the already-migrated dev databases) before ever wiring them into the compose file
  - [x] **The dashboard had no way to reach a protected route through a real browser at all** — found during the initial survey, not assumed still true from an old note: `apps/web/src/lib/api.ts`'s `apiFetch` never sent `credentials: "include"`, and there was no `/login` or `/register` route (`index.tsx`'s own comment: "no auth exists yet"). Fixed both halves — `credentials: "include"` added, plus real `login.tsx`/`register.tsx` routes and a real authenticated landing page (`index.tsx`, showing org memberships, a create-project form, and a manual project-ID nav — still not the full org/project-switcher `application-architecture.md`'s IA describes, since Application/Environment still aren't real entities and there's still no "list projects" endpoint, both unchanged pre-existing deferrals). `apps/control-api/src/lib/cors.ts` (new, `@elysiajs/cors`) makes the other half of this work — a credentialed cross-origin request needs the server to echo the caller's specific origin (never `"*"`) and set `Access-Control-Allow-Credentials`, confirmed neither side fixes this alone. Verified live: a real register → cookie → `/auth/me` round trip through actual cross-origin HTTP with an `Origin` header, not just unit tests
  - [x] **A fifth real Elysia/CORS finding**: unlike `onError` (Full auth/RBAC's own entry above), `@elysiajs/cors`'s registration position in the `.use()` chain does *not* matter — confirmed empirically registering it after `.use(api)` still produces correct `Access-Control-Allow-*` headers and a correct preflight `OPTIONS` response. Worth confirming rather than assuming given `onError`'s own order-sensitivity was just found the same session
  - [x] **Real gap found during the SDK dry-run, not assumed working**: the browser SDK had never actually been exercised against a real CORS-enforcing `fetch` before this pass — every earlier test in this repo drove it from a server-side Bun/Go test runner or `curl`, none of which enforce CORS. Dry-run used `happy-dom` (already a devDependency, used for `apps/web`'s own component tests) to get a real `window`/`fetch` and ran the actual SDK code unmodified — it failed with real CORS preflight rejections (`ingestion` had zero CORS handling; it authenticates via a bearer public key, not a cookie, so it had never needed any before). Fixed with `corsMiddleware` in `cmd/ingestion/main.go` — `Access-Control-Allow-Origin: "*"`, not an allowlist (unlike control-api): a public key is meant to be embedded in any customer's public-facing page, the same way Sentry/analytics ingest endpoints accept any origin holding a valid project key. Re-ran the identical dry-run after the fix — SDK → real ingestion → Redpanda → worker → ClickHouse → control-api's issues query, all real, all confirmed end to end (`{"issues":[{"title":"dry-run pilot readiness check",...}]}`)
  - [x] **Real, dry-run-verified installation walkthrough**: `docs/08-operations-release/self-hosted-installation.md` (new) — not written from assumption, every step actually executed against a truly fresh install (fresh volumes, fresh containers, `infra/local/` torn down first so nothing else could mask a real gap) before being documented: configure → build → start → validate health → register org/admin (real cross-origin browser-equivalent request) → create project → install SDK and send real telemetry → confirm data visible via a real issues query → configure a real alert rule → take and restore-verify a real Postgres backup (`docker compose exec postgres pg_dump`, chosen over `scripts/backup-postgres.sh` specifically because this profile doesn't expose Postgres to the host and doesn't need `PG_BIN_DIR` version-matching when the dump runs inside the same-version container)
  - [x] **Every checklist item's real result**: installation successful ✓, SDK installed ✓ (after the CORS fix), data visible ✓ (real issue appeared from real telemetry), alerts configured ✓ (real rule created), backup configured ✓ (real dump, restore-verified with matching row counts). Two items are genuine, documented gaps, not faked: **source maps** (zero code anywhere — no upload endpoint, no storage, no server-side resolution; needs an object-storage decision this project hasn't made, `docs/decisions/README.md`'s own "genuinely still open" list) and **privacy rules** (`packages/sdk/src/privacy.ts`'s `applyPrivacy()` is a literal no-op, ADR-007's placeholder-for-later-ordering by design, confirmed no config surface exists client or server side)
  - [x] **A real, found-not-assumed limitation, documented rather than silently shipped**: `control-api`'s session cookie is `Secure` whenever `NODE_ENV=production` (which the Dockerfile always sets, correctly), but this profile has no TLS-terminating reverse proxy anywhere — a real browser (unlike this dry-run's `curl`, which doesn't enforce `Secure`) may refuse to send the cookie back at all outside `localhost`'s possible special-case leniency (unverified — no real browser was available to test against in this environment). Documented explicitly in the install guide's "Known limitations," not glossed over
  - [x] Deferred, not forgotten: Kubernetes/Helm packaging (Profile B's other variant, "small Kubernetes deployment" — this pass only built the Single VM variant), a runtime-configurable `VITE_API_BASE_URL` (today's is a build-time value, documented as a real limitation), `apps/web`'s ~700MB image size (both `node_modules` trees have to ship today — a real, unstarted optimization), running `scripts/dr-exercise.sh`'s exercise against this profile specifically (it was built and verified against `infra/local/`'s profile in Step 9), an automated backup cadence (today's is the same manual-procedure status `scripts/backup-postgres.sh` already had), and the real business step of finding an actual pilot customer (the item directly above this one)
  - [x] **A sixth real bug, found the moment a real browser (via the user, not `curl`) actually loaded the dashboard**: every page hung on the SSR "Loading..." fallback forever. Root cause — `dist/server/server.js` (TanStack Start's default production entry, unmodified) exports a bare `{ fetch }` handler with *no* static-file serving of its own; confirmed by reading the built output directly (no `readFile`/`public`/`listen(` anywhere in it) rather than guessing. Bun's "default export has a `fetch`" auto-server behavior is what made `/` work at all — but every `/assets/*.js` the SSR HTML referenced 404'd against that same handler, so the client bundle never loaded, hydration never ran, and the React Query suspense fallback (`routes/index.tsx`'s `<p>Loading...</p>`) never resolved. This is a real gap in TanStack Start's plain `vite build` output itself: it assumes a deploy target (Vercel/Cloudflare Pages/Netlify) serves `dist/client` as static/CDN assets *separately* from the server function — a self-hosted Docker profile has no such layer. Fixed with `apps/web/serve.ts`, a small production entry that serves `dist/client/assets/*` via `Bun.file()` and delegates everything else to the real SSR handler; `Dockerfile`'s `CMD` now runs it instead of `dist/server/server.js` directly. Verified: every asset the SSR shell references now returns 200, `/`, `/login`, `/register` all still 200, and an unmatched `/assets/*` path still correctly 404s (not swallowed into the SSR router)

---

## Post-MVP gap closure

Found 2026-08-16 by re-reviewing `mvp.md` §4's actual MVP-capability list (not just the 10-step build order) against the real repo — six real capabilities `mvp.md`/the relevant `E*.md` user stories name as in-scope that never got tracked as deferred anywhere in Steps 1–10 above, so they'd stayed invisible. Being worked incrementally, one at a time, each fully built+tested+verified before moving to the next — not a single big pass.

- [x] **Privacy/redaction (US-16.01, US-03.04, ADR-007)** — `packages/sdk/src/privacy.ts`'s `applyPrivacy()` was a literal no-op since Step 3; it's the first of these six items because the pilot readiness checklist (Step 10) explicitly names "privacy rules configured" as a real gap, not a hypothetical one
  - [x] Built-in rules, always on, not configurable off (`privacy-and-security.md`: "never collected by default: passwords, authentication tokens, payment credentials") — email addresses, `Bearer` tokens, `password`/`token`/`secret`/`api_key`/`authorization`-shaped key:value fragments (JSON- and query-string-shaped both), payment-card-length digit runs (deliberately not Luhn-checked — over-redacting costs nothing, under-redacting a real card number does)
  - [x] `PrivacyConfig.customRules` — organization-specific patterns on top of the built-ins, `privacy-and-security.md`'s own example (`customerAccountNumber -> [REDACTED]`) made real. `PrivacyConfig`/`PrivacyRule` exported from the package's public surface (`index.ts`) — a real, if small, second gap found while wiring this: `InitOptions` itself wasn't exported either, so nothing outside `client.ts` could even name the type to pass a typed `privacy` option
  - [x] Fail-closed (US-03.04: "redaction failures fail closed for protected fields") — a rule that throws mid-evaluation redacts the *whole field* it was checking, not whatever partial `.replace()` result existed at the point of failure. Proven with a rule constructed to genuinely throw via `Symbol.replace` (not simulated/assumed) — confirmed the already-partially-redacted string never leaks through
  - [x] Applied to every string field this SDK actually captures today: error `message`/`stackTrace`, the network `resource` path, and `context.route` (uniformly across all three event types, including performance events, which have no sensitive string field of their own but still route through for the shared `context.route` redaction) — **not** `redactHeaders`/`maskText`/`maskInputs` from `privacy-and-security.md`'s config example, since this SDK doesn't capture headers, DOM text, or input values at all yet (no breadcrumb/interaction instrumentation exists — that's this list's next item). Wiring those flags in now would be config that silently does nothing; deferred honestly, not faked
  - [x] 15 new tests (`privacy.test.ts`) — every built-in rule, custom-rule composition/ordering/custom-replacement, the fail-closed throw case, and that non-string/non-sensitive fields (`eventId`, performance payloads) pass through unchanged. 52 sdk tests passing (was 37), stable across 3 consecutive runs. `biome check` + `tsc --noEmit` clean
  - [x] **Real end-to-end verification, not just the unit-tested function in isolation**: ran the actual SDK (`happy-dom`, same technique Step 10's CORS dry-run used) through real `init()` → `captureException()` → `client.ts` → `privacy.ts` → `serialize.ts` → `transport.ts`, intercepting the real outgoing `fetch` to `/ingest/v1/events` — confirmed the literal wire payload has the email/card/token/custom-account-number all redacted, not just the value returned by calling `applyPrivacy()` directly. No Go/wire-contract changes needed here (pure browser-side layer, same reasoning Step 3's original skeleton verification used, not the fuller live-services pass changes touching the wire format get)
  - [x] `apps/demo` gained a "Capture an error with sensitive data" button so this is exercisable by hand too, same pattern as the existing unhandled/handled-error buttons — not part of CI (matches `apps/demo`'s existing non-CI status)
  - Deferred, not forgotten: `redactHeaders`/`maskText`/`maskInputs` (blocked on breadcrumb/interaction/header capture existing at all — next item below), regex-based custom rules with catastrophic-backtracking potential (ReDoS) aren't specifically guarded against — a genuinely pathological custom regex could still hang synchronously; noted as a real, unsolved edge case rather than silently assumed safe, server-side redaction (ADR-007's defense-in-depth second layer — still doesn't exist, unchanged by this pass), auditability of redaction config (US-16.01's "configuration is auditable" — no admin surface for privacy config exists at all yet, it's still `init()`-time-only)
- [x] **Breadcrumbs (E07, P0)** — real design decision confirmed with the user before building (same "ambiguous scope, ask first" precedent Step 8's alert-type scoping set): breadcrumbs are a bounded trail **attached to an error's own payload** (Sentry's "what led up to this" model), not a fourth standalone telemetry event type. Smaller footprint, no wire-contract/Go/ClickHouse pipeline changes, and it directly serves E07's actual stated purpose ("so I can see where the user was before a failure") — the alternative (a real 4th event type) is deferred, tracked below, not silently dropped
  - [x] `packages/sdk/src/breadcrumbs.ts` — bounded ring buffer (last 50, `instrumentation.md`'s own "storage is bounded" requirement), `recordBreadcrumb()` (internal, used by every automatic source below) + `addBreadcrumb()` (the public manual API, `instrumentation.md`'s `frontwatch.addBreadcrumb(...)`, always category `"custom"`) + `getBreadcrumbTrail()` (returns a copy, never the live array)
  - [x] `event.ts` gained `Breadcrumb`/`BreadcrumbCategory` (all six categories from `instrumentation.md`, even though `"performance"` isn't emitted automatically this pass) and `ErrorPayload.breadcrumbs?: Breadcrumb[]`
  - [x] `packages/sdk/src/navigation.ts` (new) — patches `history.pushState`/`replaceState` + listens for `popstate`, records a `"navigation"` breadcrumb only on a genuine path change (never the first page load, never a no-op state change to the same path, never the query string) — framework-independent by design (ADR-005), same reasoning `context.ts`'s own `route` field already documents. A real, useful step toward this list's still-open "Navigation as its own instrumentation" item below, **not** a silent substitute for it — a breadcrumb isn't a queryable event
  - [x] `packages/sdk/src/interactions.ts` (new) — a single global, capturing, passive click listener records an `"interaction"` breadcrumb describing *what kind of element* was clicked (tag + id + first CSS class only) — never `innerText`/`value`/`aria-label`, a **structural** guarantee (never collected in the first place) rather than something privacy.ts's redaction has to catch after the fact, satisfying US-07.02's "sensitive input values are never captured by default" by construction
  - [x] `network.ts` gained a `"network"` breadcrumb (`"GET /accounts/:id -> 200"`, same normalized resource + the format `db/sessions.ts`'s own session-summary building already established) on both the success and failure paths
  - [x] `client.ts`'s `captureException()`: reads the current trail *before* recording this error as a new breadcrumb — so an error's own trail describes what led up to it, never includes itself, while still being visible in a *later* error's trail. Omits `breadcrumbs` entirely (not `[]`) when there's nothing to attach yet
  - [x] `privacy.ts` extended (not a separate pass) — every breadcrumb's `message` goes through the same redaction rules as the error message itself, and every *string-valued* key in a breadcrumb's free-form `data` object too (`instrumentation.md`: "same privacy rules as any other telemetry apply") — reuses gap 1's `redactString`/fail-closed machinery directly, no new redaction logic
  - [x] Wire contract: `packages/contracts/src/telemetry.ts` gained `WireBreadcrumb`/`WireBreadcrumbCategory` + `WireErrorPayload.breadcrumbs` — still **not** a new `WireEventType` member, matching the design decision above. Go's `ErrorPayload` gained the matching `Breadcrumbs []Breadcrumb` field — needed zero changes to `Validate()`/`MarshalJSON`/`UnmarshalJSON`/the ClickHouse writer, since all four already switch generically on `EventType` via `PayloadJSON()`
  - [x] **Real compile-time breakage caught immediately, not a runtime surprise**: adding a slice field to Go's `ErrorPayload` makes the struct no longer comparable with `==`/`!=` — `event_test.go`'s existing round-trip test used exactly that comparison and failed to *compile*. Fixed with `reflect.DeepEqual`, plus a new dedicated round-trip sub-test for a payload carrying two real breadcrumbs (including a `data` map, where JSON round-tripping numbers through `interface{}` requires comparing against `float64`, not `int` — a real Go/JSON gotcha, not guessed)
  - [x] Dashboard: `IssueDetail.tsx` renders each occurrence's trail via a `<details>` disclosure (compact by default, one click away — E07.03's "View Timeline" scoped to *this* occurrence, not a separate page/session-wide view). `db/issues.ts`'s occurrence query gained `JSONExtractRaw(payload, 'breadcrumbs')`, parsed defensively (`parseOccurrenceBreadcrumbs`, fails soft to `[]` on anything unexpected — proven against a real wrong-shaped-but-valid-JSON payload, not a contrived one, since a genuinely malformed JSON fragment can't exist without also breaking the surrounding payload's own validity, which is a different, pre-existing code path this change didn't touch). New `OccurrenceBreadcrumb` type in `packages/contracts`, kept separate from the SDK's own `Breadcrumb` type (contracts isn't an sdk dependency)
  - [x] **Real, hard Bun testing limitation found and worked around, not fought further**: `client.ts`'s new breadcrumb-attaching logic has never had direct unit test coverage (nothing in this codebase ever unit-tested `Client.captureException` directly before this pass, only indirectly via `errors.ts`'s mocked `"./client"`). Attempted a dedicated `client.test.ts` importing the *real* `Client` class — confirmed empirically (not assumed) that `network.test.ts`/`performance.test.ts`'s `mock.module("./client", ...)` calls poison the shared module registry for *any* file needing the real module, regardless of alphabetical file ordering (`client.test.ts` sorts before all three and still got the mocked version), and that a query-string cache-busting import (`"./client?real"`) does not bypass Bun's module-mock resolution either — genuinely confirmed by testing it, not assumed to fail. Deleted that file; covered instead by stubbing `globalThis.fetch` (transport.test.ts's own established pattern) — sidesteps `"./client"` as a module identity entirely, so it can't collide with the other files' mocks regardless of execution order
  - [x] 24 new SDK tests (`breadcrumbs.test.ts`, `navigation.test.ts`, `interactions.test.ts`, plus extensions to `privacy.test.ts`/`event.test.ts`/`serialize.test.ts`/`network.test.ts`) — 76 total (was 52), stable across 5 consecutive local runs. **Another real, previously-undocumented Bun/happy-dom gap found**: the shared `happydom.ts` global registration has no URL at all (defaults to `about:blank`), against which `pushState`/`replaceState` with a relative path silently never updates `window.location.pathname` — confirmed directly with a throwaway probe before assuming it was a bug in `navigation.ts` itself. Fixed test-side only, via `window.happyDOM.setURL(...)` scoped to `navigation.test.ts`'s own `beforeEach`, not by changing the shared preload config every other SDK test file also relies on
  - [x] **Real cross-file test-isolation bug caught only by CI, not five consecutive local runs**: `breadcrumbs.test.ts`/`interactions.test.ts`/`network.test.ts` originally reset the shared `breadcrumbs.ts` trail in `afterEach` only — locally harmless (these files happened to run in an order where nothing preceded them with leftover state), but CI's file execution order differs, and `interactions.test.ts`'s first test failed there with 3 breadcrumbs recorded instead of 1 (residue from whichever file ran immediately before it). Fixed by resetting in `beforeEach` too, everywhere the shared trail is touched — the same "don't trust file execution order" lesson `errors.test.ts`'s `mock.module()` gotcha already taught this codebase once, now applied to shared *state*, not just shared *mocks*
  - [x] New `db/issues.test.ts` coverage (real ClickHouse): a real breadcrumb trail attached to an occurrence round-trips correctly, and a wrong-shaped-but-valid `breadcrumbs` value fails soft to `[]` rather than throwing. New `IssueDetail.test.tsx` coverage: no `<details>` element renders when there are no breadcrumbs, the trail renders and is findable when there are. 154 control-api tests passing (was 152), 44 web tests passing (unchanged count, existing occurrence fixtures updated for the new required field) — `biome check` + `tsc --noEmit` clean across all 4 TS packages, `go build`/`go test`/`gofmt`/`golangci-lint` clean
  - [x] **Real end-to-end verification, the whole feature working together, not just each piece in isolation**: `happy-dom` script exercising the real, unmocked pipeline — a real `pushState` navigation, a real DOM click, a real `fetch` call, a manual `addBreadcrumb()`, then a captured error containing a real email address — confirmed the literal wire payload sent to `/ingest/v1/events` carried all four breadcrumbs in the correct order (navigation → interaction → network → custom) attached to the error, with the email in the error's own `message` correctly redacted
  - Deferred, not forgotten: breadcrumbs as a genuinely standalone, independently-queryable event type (this pass's confirmed-with-the-user scope decision, not an oversight); XHR/keyboard/scroll/focus interaction sources (click-only this pass, same "cover the common case first" split `network.ts`'s fetch-only scope already used); ReDoS protection for custom privacy rules applied to breadcrumb data (unchanged, pre-existing gap from gap 1); ClickHouse's `JSONExtractRaw`-based occurrence lookup not yet paginated/cursor-based beyond the existing `RECENT_OCCURRENCES_LIMIT` (pre-existing Step 6 scope, unchanged)
- [x] **Health dashboard (US-12.01, P0)** — one composed overview aggregating the four surfaces that already existed separately, per US-12.01's acceptance criteria (error/performance/network/release health all represented, the time window is clear)
  - [x] `apps/control-api/src/db/applicationHealth.ts` (new) — `getApplicationHealth(projectId, windowMinutes = 60)`. Reuses `listPerformanceMetrics` directly (already the right shape); writes two small dedicated ClickHouse aggregates for errors (current + previous window counts, for a trend, plus distinct-fingerprint issue count) and network (request/failure counts) rather than summing `listNetworkResources`' per-`(method,resource)` rows — same "a global aggregate doesn't need a finer breakdown" reasoning `getReleaseHealth`'s own network query already established. Latest release comes straight from Postgres (`releases`, `ORDER BY deployedAt DESC LIMIT 1`), independent of telemetry status — a release can be registered before any telemetry tagged with it ever arrives
  - [x] **`telemetryStatus: "healthy" | "no_telemetry" | "stale"`, built as a real, tested three-way distinction, not an afterthought** — `health-monitoring.md`'s own explicit requirement: "a lack of data must never silently render as healthy." `no_telemetry` (zero events ever — detected via ClickHouse's `max()`-over-empty-input sentinel, `1970-01-01`, not an empty result set) and `stale` (real telemetry exists, but none in the current window) both return every telemetry-derived field as `null`, not `0` — reporting zeros would look identical to "checked the window, found nothing wrong." A real bug this exact distinction caught in its own first test run: the initial implementation only null'd fields for `no_telemetry`, not `stale` — the dedicated `getApplicationHealth` test for the stale case failed immediately against the real (buggy) behavior, not a hypothetical one
  - [x] `GET /projects/:projectId/health` (`?windowMinutes=`, default 60) — same RBAC-enforcement shape as every other project-scoped route (`viewer` role), added to `routes/tenantIsolation.test.ts`'s cross-org 404 coverage alongside the rest
  - [x] `apps/web`: `/projects/:projectId/health` (`HealthOverview`) — three genuinely different renders for the three `telemetryStatus` values, not one layout with a swapped label. Reuses `PerformanceList.tsx`'s own `formatValue`/`formatPercent` helpers (newly exported) rather than a second copy of the CLS-is-unitless rule. Links out to Issues/Network/Performance/the release detail page for full drill-down rather than duplicating their content — this page is the overview, not a replacement. Seven-way cross-nav completed across Issues/Network/Sessions/Performance/Releases/Alerts/Health; the landing page's "create project"/"go to a project" flows now route into Health first (matching `health-monitoring.md`'s framing of it as the natural entry point), not Issues
  - [x] New tests: 8 `db/applicationHealth.test.ts` cases (all three `telemetryStatus` values against real Postgres+ClickHouse with genuinely relative-to-now timestamps — a fixed historical timestamp can't exercise "is this stale relative to the current window" meaningfully, unlike `releaseHealth.test.ts`'s fixed-timestamp pattern; error trend window-boundary correctness; distinct-fingerprint `issueCount` vs. raw occurrence count; a real network failure-rate computation; latest-release surfacing independent of telemetry status), 5 route tests, 1 tenant-isolation case, 5 `HealthOverview.test.tsx` cases — 168 control-api tests passing (was 154), 49 web tests (was 44). `biome`/`tsc` clean across all 4 TS packages
  - [x] **Real test-hygiene bug caught immediately by the tests themselves, not CI this time**: the first test run's Postgres cleanup only tracked the *last* seeded organization (each test seeds its own project+org), leaking every earlier one and then failing outright on the FK-enforced `projects.organizationId` constraint when it tried to delete an organization whose project still existed. Fixed by tracking every created organization id and deleting projects (via `inArray`) before organizations, not after
  - [x] **Real end-to-end verification against the live stack**: started the real `control-api` server (against `infra/local`'s Postgres/ClickHouse — the self-hosted Docker profile's own `control-api` container was briefly stopped to free port 3000, then restarted afterward, exactly as found), registered a real org/project via HTTP, confirmed `no_telemetry` for real, inserted real error+network events directly into ClickHouse and registered a real release, confirmed `healthy` with correct real aggregates (error count 1, issue count 1, network failure rate 0, latest release `4.2.0`) via a direct `curl` to the live endpoint
  - [x] **Real, pre-existing dashboard limitation found while trying to verify the rendered page via `curl`, not caused by this change**: every protected page's SSR — confirmed identically on the *existing* Issues page, not just the new Health page — throws `ApiError: not authenticated` under a raw `curl` with a replayed cookie and falls back to client-only rendering (a real, working React resilience path, not a crash) — because the SSR-side `fetch` to `control-api` has no live browser cookie jar to inherit from, and nothing in `lib/api.ts` forwards the incoming request's `Cookie` header into that server-side call. A real browser's own subsequent client-side fetch (which *does* attach cookies) is what actually renders these pages today; this is why Step 10's own dry-run already used a real browser-equivalent request rather than raw `curl` for anything post-auth. Not a regression, not chased further — logged here since it hadn't been written down as a standing, dashboard-wide limitation before
  - Deferred, not forgotten: US-12.02's health *trends over time* (this pass surfaces one point-in-time snapshot plus a single previous-window comparison for errors only, not a real historical chart) and *filtering by environment* (blocked on Environment not being a real entity — this list's own still-open item below); a configurable staleness threshold independent of `windowMinutes` (currently the same window doubles as both "what to aggregate" and "how recent counts as healthy" — a real, simplifying assumption, not hidden)
- [x] **SSR-cookie-forwarding fix** — closes the dashboard-wide limitation found above: `lib/api.ts`'s SSR-side `fetch` had no live browser cookie jar to inherit from, so every protected page's server render 401'd and silently fell back to client-only rendering
  - [x] `getForwardedCookie()` reads the incoming request's own `Cookie` header (`getRequestHeader("cookie")`, `@tanstack/react-start/server`) and forwards it explicitly on the outgoing server-side call — `credentials: "include"` (the browser-side fix) does nothing for a fetch with no browser cookie jar to draw from in the first place
  - [x] **Real bundler-level finding, not just a runtime bug**: a naive `typeof window === "undefined" ? getRequestHeader(...) : undefined` guard compiled and ran, but TanStack Start's own `import-protection` Vite plugin flagged it as unsafe — importing `@tanstack/react-start/server` at all from a file also reachable from client code risks the server-only module leaking into the client bundle, regardless of whether the call site is correctly guarded. Fixed with `createIsomorphicFn().server(...).client(...)` — the framework's own sanctioned primitive, confirmed via context7 against current TanStack Start docs rather than guessed — which the bundler knows to split per-environment, not just branch on at runtime
  - [x] **Real A/B verification, not assumed from the diff alone**: reverted to the pre-fix code against a live `vite dev` server pointed at a real `control-api`, replayed a real session cookie via `curl`, confirmed the exact pre-existing bug reproduces (`"Switched to client rendering because the server rendering errored: not authenticated"`) — then reapplied the fix and confirmed the same request now renders real authenticated SSR data (`<p>No issues found for this project.</p>`, the correct empty-state for a fresh project) instead
  - [x] 53 web tests unaffected (no test coverage added — this is a live-request-shaped fix, matching the "real E2E over a mocked unit test" bar the original finding itself used), `biome`/`tsc` clean
- [x] **CI flake, second pass — a real client-level hardening fix, not another connection-pool tweak**: PR #47/#49/#50 each hit the known ClickHouse-test flake at a much higher rate than the ~30% baseline from PR #42, now hanging for the *full* 20000ms timeout rather than queueing — a real stuck connection, not pool exhaustion. `@clickhouse/client`'s own troubleshooting docs (confirmed via context7, not guessed) name the exact mechanism: Keep-Alive reuses idle sockets, the server closes ones past its own timeout, and the client can be unaware and try to reuse a dead one — normally caught by the client's own `idle_socket_ttl` timer, but that timer can fire late "on CPU-starved machines," which a shared CI runner running Postgres + ClickHouse + this whole suite concurrently genuinely is. Added `keep_alive: { eagerly_destroy_stale_sockets: true }` (the client's own documented mitigation) + an explicit `request_timeout: 15000` as defense in depth (fails with a clear ClickHouse-client error well before CI's 20000ms bun:test timeout, instead of a vague test-runner timeout with no information about which layer stalled). Not verified to be the sole cause — a CI-only, load-dependent hang is inherently hard to reproduce and confirm locally — the next several PRs' real CI runs are the actual test
- [x] **SDK core SSR-crash fix** — found while scoping the framework-adapter work below (gap 6), fixed immediately rather than held for later since it's small, contained, and a real standing bug on `main`: `init()` threw `window is not defined` (confirmed with a real throwaway script outside any browser-shimmed environment, not assumed) the instant it ran outside a browser — every one of the five `register*Instrumentation()` functions (`errors`/`network`/`navigation`/`interactions`/`performance`) touched `window`/`document` unguarded. Directly violates `instrumentation.md`'s explicit "must never accidentally execute browser-specific instrumentation logic during SSR." Fixed with an identical early-return guard in all five, verified against the same reproduction script (now succeeds). New `ssr-safety.test.ts` — the only test file in this package that deletes `window`/`document`/`navigator`/`history` from `globalThis` for a scoped test and restores them immediately after, since `happydom.ts`'s shared preload makes them present everywhere else in the suite; confirmed stable across 3 consecutive full-suite runs, no cross-file leakage. 5 new tests, 81 total (was 76)
- [x] **Navigation as its own instrumentation** — a real, standalone, independently-queryable 4th telemetry event type (`error`/`network`/`performance`/`navigation`), full pipeline: SDK → Go ingestion/worker → ClickHouse → control-api aggregation → dashboard. Coexists with, not a replacement for, the "navigation" breadcrumb above — both fire from the same underlying `navigation.ts` History API patch, serving genuinely different purposes (context attached to a *future* error vs. an independently-investigable event)
  - [x] `packages/contracts`/`event.ts`: `WireEventType`/`EventType` widened to four members, `WireNavigationPayload`/`NavigationPayload` (`from_route?`/`fromRoute: string | null`, `to_route`/`toRoute`, `navigation_type`). `fromRoute` is nullable/optional (not just always a string) for wire-contract completeness, even though `navigation.ts`'s real capture path never actually produces a null in practice — it only ever fires once there's a genuine previous route
  - [x] `navigation.ts`'s `recordNavigationIfChanged()` now threads a real `navigationType` (`"push"`/`"replace"`/`"pop"`) through to both the breadcrumb and the new `captureNavigationEvent()` call, sourced from each of the three real call sites (the two wrapped History methods + the `popstate` listener), not guessed/hardcoded
  - [x] `privacy.ts` extended — `fromRoute`/`toRoute` redacted the same way `context.route` already is, reusing the existing `redactString`/fail-closed machinery
  - [x] Go `internal/telemetry`: `NavigationPayload.FromRoute` is a `*string`, not a plain string with `omitempty` — a real, deliberate choice (US-03.04-adjacent correctness, not Go idiom for its own sake): an empty string and "no prior route at all" are genuinely different states, and a plain string can't represent the difference the way `PerformancePayload.Value`'s own "zero isn't absent" precedent already established for a different field
  - [x] **Real compile-time break caught immediately by tsc/go build, not left to a test failure**: `serialize.ts`'s existing catch-all fallback return (previously implicitly "whatever's left after network/performance is error") would have silently mis-serialized every navigation event as an error payload — TypeScript's own discriminated-union narrowing refused to compile `event.payload.message` once a 4th union member existed, forcing an explicit navigation branch rather than letting this ship broken
  - [x] `GET /projects/:projectId/navigation` (new `db/navigation.ts`, GROUP BY `(from_route, to_route)`, same ADR-023 shape as `network.ts`'s `(method, resource)`) + `apps/web`'s `/projects/:projectId/navigation` (`NavigationList`) — eight-way cross-nav completed across every project-scoped page
  - [x] **Real, previously-untracked bug found and fixed in the same pass, not left as a second half-measure**: `db/sessions.ts`'s `TimelineRow`/`summarize()` union was `"error" | "network"` only — "performance" was never handled either, even though performance events have carried a `session_id` since Step 7. A performance event landing in a session's timeline silently fell through to the network-shaped branch, printing `"? ? -> ?"` (confirmed as the actual pre-existing failure mode via a real test, not assumed). Both `performance` and `navigation` given real summary branches (`"LCP: 1800"`, `"Navigation -> /settings"`); `SessionEvent.eventType` widened to all four types in `packages/contracts`
  - [x] 12 new SDK tests (93 total, was 81 — includes the `mock.module("./client", ...)` shape update in `errors.test.ts`/`network.test.ts`/`performance.test.ts`, needed since `navigation.ts` now imports `captureNavigationEvent` from the same shared-registry module those files replace), 4 new Go tests (`Validate()`, JSON round-trip including the nil-`FromRoute` case, a real ClickHouse integration test), 13 new control-api tests (181 total, was 168 — `db`/`route`/tenant-isolation/session-timeline-fix coverage), 4 new web tests (53 total, was 49). `biome`/`tsc` clean across all 4 TS packages, `go build`/`test`/`gofmt`/`golangci-lint` clean
  - [x] **Real end-to-end verification, the full live pipeline**: real local Redpanda/ingestion/control-api started (briefly swapping out the self-hosted Docker profile's own containers occupying the same ports, restored afterward exactly as found), a real project registered via HTTP, a real navigation event sent through `/ingest/v1/events` — confirmed it landed in ClickHouse with the correct payload, confirmed `GET /navigation` aggregated it correctly (`transitionCount: 1`), and confirmed the session-timeline fix for real (`"summary": "Navigation -> /settings"`, not the old bug's `"? ? -> ?"`)
  - Deferred, not forgotten: a per-route "top entry/exit pages" or funnel-style view (this pass is a flat transition list, not a graph); `SessionSummary`'s own count fields (`errorCount`/`networkCount`) weren't extended with `navigationCount`/`performanceCount` — a real, deliberately narrow scope decision (the timeline *rendering* bug needed fixing regardless of this work; the summary *aggregate counts* are a separate, un-scoped addition)
- [x] **Applications/Environments as real entities** — the riskiest of the six, per the user's own flag before starting: real, tested change, safely scoped after a real design-fork confirmation, not the full close
  - [x] **Scope confirmed with the user before touching anything, the biggest fork of the six gaps**: data-model.md §1's real hierarchy (`Organization → Application → Environment → Project`) could be closed two genuinely different ways — make `projects.application_id`/`environment_id` *required* (fully closes the gap, but breaks every existing project-creation call site: control-api tests, `loadtest/provision.sh`, `chaostest/`, `apps/demo`, the self-hosted install docs) or make Application/Environment *real tables with real FK constraints, kept optional* (real progress, zero blast radius, doesn't fully close the letter of the gap). Went with the optional/safe option — confirmed empirically first that this was actually safe: `SELECT count(application_id), count(environment_id) FROM projects` on real local data returned `0, 0` for all 8 existing rows, not assumed
  - [x] `applications`/`environments` Postgres tables (Drizzle schema + migration) — `applications.framework` is free text, not an enum (`mvp.md` §5's Tier 1/2/3 framework list is still growing); `environments.type` (`development`/`staging`/`production`/`custom`, data-model.md's own literal list) has **no default** — a deliberate choice, since silently defaulting an unspecified environment to `"production"` would be actively dangerous, unlike `alertRuleType`'s own `"new_issue"` default (a genuine common-case, not a dangerous guess)
  - [x] `projects.application_id`/`environment_id` gained real `.references()` FK constraints, still nullable — `POST /projects` with just an `organizationId` keeps working exactly as before, verified by re-running the full existing test suite (nothing broke) rather than assumed from the schema alone
  - [x] `lib/authorization.ts` gained `authorizeApplicationAccess`/`authorizeEnvironmentAccess`, same shape as the existing `authorizeProjectAccess` one level up the hierarchy — 404 (not 403) for cross-org, same tenant-isolation reasoning already established
  - [x] `POST /applications` + `GET /organizations/:organizationId/applications` + `GET /applications/:applicationId`; `POST /environments` + `GET /applications/:applicationId/environments` + `GET /environments/:environmentId` — same RBAC-enforcement shape as every other route (`engineer` to create, `viewer` to read)
  - [x] **Real tenant-isolation gap found and closed while wiring `POST /projects` to actually validate the ids it's given, not just trust them**: a real FK constraint alone rejects a genuinely nonexistent `applicationId`, but not a *valid* id belonging to a *different* organization — without an application-level check, an engineer in org A could silently attach their new project to org B's application. `POST /projects` now verifies the application belongs to the claimed organization and, if an environment is also given, that it belongs to that same application — a mismatched-but-real pair (env of app B, claiming app A) is rejected too, not just a wholly foreign one. Real `422`, not a raw Postgres FK-violation 500
  - [x] New tests: 11 `db/applications.test.ts` + `db/environments.test.ts` cases (real Postgres), 13 route tests, 3 new `POST /projects` validation cases (real cross-org application, mismatched app/environment pair, and the success path with both set), 4 new tenant-isolation cases — 212 control-api tests passing (was 181). `biome`/`tsc` clean, `go build` unaffected (this gap is pure control-plane, no Go/ClickHouse/SDK changes at all)
  - [x] **Real end-to-end verification against the live stack, control-plane only** (no ClickHouse/Go pipeline involved — pure Postgres CRUD, so this is the right-sized bar, matching Step 2's own original precedent rather than the fuller multi-service checks Steps 7/App-Health/Navigation needed): the self-hosted Docker profile's `control-api` container briefly swapped out for a live dev instance (restored after, exactly as found), a real org → application → environment → project chain created through actual HTTP, and both tenant-isolation rejections (`422` for a cross-org `applicationId`, `404` for reading another org's application) confirmed for real, not just via the route-level test suite
  - Deferred, not forgotten, exactly as scoped upfront: `application_id`/`environment_id` becoming *required* on `projects` (the "full close," a real breaking change touching every project-creation call site — a deliberate, separate decision for when there's an actual multi-environment customer need, not a hole left by accident); any dashboard UI for creating/browsing Applications/Environments (this pass is backend-only, matching how the scope conversation with the user focused on "real tables + CRUD + FK integrity," not an IA change); `AlertRule`/`Release`/`Event` gaining real `application_id`/`environment_id` columns of their own (data-model.md's fuller vision — `project_id` remains the only real tenant/telemetry boundary those still use, unchanged by this pass)
- [ ] **Framework adapters (Tier 1: React, React Router, TanStack Start)** — scope confirmed with the user: a real `packages/react` (Error Boundary + SSR-safe init hook), not the full Loader/Action/Server-Function spec — no adapter package exists yet; `apps/web`/`apps/demo` both import the SDK core directly. **Update 2026-08-17**: built (`feat/react-adapter` branch, PR #49) — see that PR's own description for the full writeup once merged; not duplicated here to avoid the two branches' PROGRESS.md edits fighting each other before merge

### Additional gaps found in a fresh end-to-end review (2026-08-17)

A second pass, this time reading `E15-search.md`/`E16-privacy.md`/`E18-frameworks.md` directly rather than trusting `mvp.md` §4's summary alone, found three more real P0 gaps the first pass missed. Confirmed by grepping the actual dashboard code, not assumed: no list page has a time-range control at all (`HealthOverview.tsx`'s window is a fixed default, displayed as text, not a selector), no environment filter exists anywhere (consistent with Applications/Environments having no dashboard surface — gap 5 above), release filtering is backend-only on 2 of 5 relevant endpoints with zero UI for it, and browser/device filtering is structurally impossible since UA parsing was never built (Step 3/7's own deferral, never revisited). Being worked the same way as the original six — incrementally, real verification each time.

- [x] **US-16.02 Configure Collection** — `init()` unconditionally registered all five instrumentation categories; no way for an app to disable one (e.g. a privacy-sensitive app turning off network capture)
  - [x] `CaptureConfig` (`client.ts`) — `{ errors?, network?, performance?, navigation?, interactions?: boolean }`, every key defaulting to `true`. `index.ts`'s `resolveCaptureConfig()` pulled out as its own pure, exported function specifically so it's directly unit-testable without mocking any of the five `register*Instrumentation` imports — mocking those at the module level was tried first and found to actually break `errors.test.ts`/`network.test.ts`/`performance.test.ts`'s own direct tests of the real implementations, since `mock.module()` replaces a specifier in bun:test's *shared* registry for the whole run, not scoped per file (confirmed the hard way, not assumed)
  - [x] **A second, real pre-existing gap found while adding this**: none of `errors.test.ts`/`network.test.ts`/`navigation.test.ts`/`performance.test.ts`'s existing `mock.module("./client", ...)` calls stubbed `init` — never needed before, since nothing previously imported the real `./index.ts` → `./client.ts` chain from a test file. `index.test.ts` was the first to do that and failed with `SyntaxError: Export named 'init' not found` (an earlier file's incomplete mock had already replaced `./client` in the shared registry by the time it ran) — fixed by completing all four mocks' shapes, the same "keep every mock of a shared module complete" lesson this codebase already learned once for `captureNetworkEvent`/`capturePerformanceEvent`/`captureNavigationEvent`
  - [x] **A third, real and genuinely intermittent bug found and fixed while stabilizing this suite, unrelated to the feature itself**: `ssr-safety.test.ts`'s `window`/`document` deletion relied on a shared `afterEach` to restore them — reproduced several times, then stopped reproducing, then confirmed for real via `git stash`/`git stash pop` bisection that it's a genuine race, not imagined: bun:test wraps every test body in its own internal promise handling even for fully synchronous tests, enough of a scheduling gap for another file's test to observe `window`/`document` mid-deletion if bun interleaves file execution. Fixed by moving the delete/restore into a synchronous `try/finally` *inside* each test instead of a separate hook — closes the gap entirely rather than narrowing it, since a `finally` block has no yield point between the test body and cleanup the way a distinct `afterEach` hook does. 10 consecutive clean runs after the fix, versus reproducible failures before it
  - [x] 5 new tests (`resolveCaptureConfig`'s own unit tests), 98 sdk tests total (was 93 before this pass, 81 before gap 6's own work). `biome check` + `tsc --noEmit` clean
  - [x] **Real end-to-end verification**: `happy-dom` script (unmocked) with `capture: { network: false }`, then a real `fetch()` call followed by a real `captureException()` — intercepted the real outgoing ingestion requests and confirmed exactly one (the error), never a network event, despite `fetch()` genuinely being called
- [x] **US-16.03 Configure Retention** — no retention policy existed anywhere; the `events` table grew unboundedly forever, an explicitly-deferred decision since Step 8's backup work ("depends on retention/durability requirements")
  - [x] `services/data-plane/migrations/clickhouse/0002_events_retention_ttl` — ClickHouse `TTL` on `events`, 90-day documented default (not spec'd anywhere in this repo, a reasonable common default). Scoped deliberately to ClickHouse only: Postgres holds control-plane data (`projects`/`releases`/`alert_rules`/`alert_events`), not telemetry (`data-model.md`'s own classification), so it's out of scope for *this* policy
  - [x] **Real bug found and fixed, not assumed correct**: the first version's TTL expression used the raw `client_timestamp` column directly — ClickHouse rejected it outright (`BAD_TTL_EXPRESSION`: "TTL expression result column should have DateTime or Date type, but has DateTime64(3)"), confirmed by actually running it against a real instance, not just writing DDL and trusting it. Fixed with `toDate(client_timestamp)` — the same expression the table is already partitioned by (`PARTITION BY toDate(client_timestamp)`), which is also the efficient path: a whole aged-out partition can be dropped directly rather than deleted row by row
  - [x] **A second, real and genuinely confusing bug found while getting the migration to actually apply**: `golang-migrate`'s ClickHouse `x-multi-statement` driver failed with a misleading `Empty query` error on a *syntactically correct* statement — bisected empirically (not guessed) down to the em dash/smart-quote/backtick characters in the migration file's own leading SQL comment confusing the driver's statement splitter, a real parser fragility in the migration tool itself, unrelated to ClickHouse. Fixed by keeping migration file comments to plain ASCII punctuation only — a real, previously-undocumented constraint on every future ClickHouse migration in this repo, not just this one
  - [x] `scripts/set-clickhouse-retention.sh` — the actual operator-facing "configure" half of US-16.03 (`DAYS=<n>`, validates a positive integer, talks to ClickHouse's HTTP interface directly via `curl`, no `clickhouse-client` dependency to install or document), same tooling-maturity tier as `scripts/backup-postgres.sh`. `shellcheck` clean. Hit the identical raw-column TTL bug independently while testing it live — fixed the same way
  - [x] **Real end-to-end verification against a real ClickHouse instance, every step, not assumed from the DDL alone**: ran the actual migration up (confirmed the `TTL` clause via `SHOW CREATE TABLE`), ran it back down (confirmed the clause is gone), ran it up again, then ran `scripts/set-clickhouse-retention.sh` for real (30 days, confirmed via `SHOW CREATE TABLE` again) before resetting to the documented 90-day default. Also ran `apps/control-api`'s full 212-test suite against the now-TTL'd table to confirm no existing test (including ones seeding historical/backfilled timestamps) is affected — TTL only prunes during background merges, never at `SELECT` time, so freshly-inserted old-timestamped test fixtures stay immediately queryable exactly as before
  - Deferred, not forgotten: per-organization/per-project retention (this is one global setting for the whole ClickHouse instance — real per-tenant retention would need either per-row TTL expressions joined against a control-plane setting, which ClickHouse's TTL syntax can't do directly, or a scheduled deletion job, neither built); a scheduled/automated retention-cadence audit; Postgres control-plane retention (explicitly out of scope, see above)
- [x] **US-15.01/US-15.03 Time-range and release filtering** (the two E15 sub-parts that close without new structural work — environment and browser/device filtering stay open, see below)
  - [x] **Real, previously-latent backend bug found and fixed first, before any UI could even work**: `db/{issues,network,sessions,performance,navigation}.ts`'s `from`/`to` filters bind straight to a `{from:DateTime64(3)}` ClickHouse query param — the exact "rejects ISO-8601, wants its own `YYYY-MM-DD HH:MM:SS.mmm`" gotcha this codebase had already hit twice before (Step 5's insert path, Step 8's `since` param), just never triggered here because no real caller had ever sent a real `from`/`to` value through this path. `db/clickhouse.ts`'s new `parseClickHouseTimeRangeQuery()` converts a real HTTP-caller-shaped ISO-8601 query param before it reaches the db layer — wired into all five routes. `network.ts`/`performance.ts` also had `release` supported at the db layer but never exposed as an actual route query param at all — fixed the same pass
  - [x] `apps/web/src/lib/timeRange.ts` — five named presets (1h/24h/7d/30d/all), no custom date-range picker yet (not built speculatively ahead of an actual need for one, same "real v1, not the full acceptance-criteria letter" scoping this project uses elsewhere)
  - [x] `apps/web/src/components/FilterBar.tsx` — one shared component (time-range `<select>` + a release text input, committed on blur/Enter not every keystroke) reused across Issues/Network/Performance/Navigation/Sessions rather than five hand-rolled bars, via `apps/web/src/lib/filterQueryString.ts`'s single `buildFilterQueryString()`. Sessions hides the release control (`showReleaseFilter={false}`) — its own backend filter set is `from`/`to` only, a session spans potentially many releases
  - [x] Every list feature's `queryOptions` now takes `filters` as a real parameter and includes it in the query key (`["issues", projectId, filters]`, not just `projectId`) — two different filter selections never collide in the cache, same pattern `application-architecture.md` already documents for every other filter this codebase has added
  - [x] 6 new `FilterBar.test.tsx` cases (immediate commit on time-range change, debounced-to-blur/Enter release commit, no-op when unchanged, hidden when `showReleaseFilter` is false), 4 new `db/clickhouse.test.ts` cases for `parseClickHouseTimeRangeQuery` (real ISO conversion, unset, unparseable, independent from/to handling), 2 new `network.test.ts` route cases (a real HTTP `release` query param, a real HTTP ISO-8601 `from`/`to` query param — end-to-end through the route, not just the pure function). 65 web tests (was 53 before this session's earlier work), 223 control-api tests (was 212). `biome`/`tsc` clean across both packages
  - [x] **Real end-to-end verification against the live stack, not just the test suite**: registered a real org/project through a live `control-api`, seeded two real ClickHouse events with different releases and timestamps, confirmed via direct HTTP against the real running API: no filter returns both, `?release=4.2.0` returns only the matching one, `?from=<real ISO-8601 timestamp>` returns only the matching one — the exact query shape the dashboard's own `FilterBar` now sends
  - [x] **Real, unplanned confirmation of a different feature along the way**: the first seed attempt used a January 2026 timestamp for the "old" event and it silently never appeared in ClickHouse at all — not a bug in this work, but the 90-day retention TTL (US-16.03, above) correctly pruning data older than its policy, live, for the first time since that migration landed. Re-seeded with a realistic ~1-week-old timestamp instead
  - Deferred, not forgotten: **environment filtering** (blocked on event-level `environment_id` tagging, which gap 5 explicitly scoped out — `Application`/`Environment` are real Postgres tables but `events` in ClickHouse still isn't tagged with either) and **browser/device filtering** (blocked on real User-Agent parsing, never built, a gap open since Step 3/7); a real date-range picker (presets only for now); filters don't persist across a page reload or via a shareable URL yet (component-local `useState`, not a TanStack Router search param) — a real, deliberate v1 scope choice, not an oversight
- [x] **CI flake: `apps/control-api`'s ClickHouse-backed tests intermittently time out at ~5000ms** — root-caused and largely fixed (PR #42): a module-level singleton `@clickhouse/client` shared its default 10-connection pool across a test suite that's grown a lot since that file was first written. Raised `max_open_connections` to 50 (primary fix) + CI's `bun test` timeout to 20000ms (defense in depth). Materially better (4 simultaneous 5000ms failures on one run → 1 occasional 20000ms one) but **not fully eliminated** — the residual failures concentrate specifically in `listNetworkResources`'s `quantile()`-based aggregation query, which looks like genuine query cost / ClickHouse merge pressure under CI load rather than pure connection-pool exhaustion. Real profiling of that query under CI-like load would be needed to close this the rest of the way; not chased further per the user's explicit call to deprioritize it below the six gaps above
  - **2026-08-16/17 update**: PR #47 (control-plane-only, zero ClickHouse involvement) hit this flake **4 times in a row** before landing clean on the 5th — a materially higher rate than the ~30% baseline measured when #42 was written, and no longer confined to `listNetworkResources` specifically (also seen in `getRecentMetricWindow`, `listIssues`, `getSession`'s new navigation/performance timeline tests — a different, unrelated test each time, always the same exact 20000ms signature). Confirmed each time that the failing test was never one this PR actually touched. Worth a real look if the rate stays this high, not just written off as the same known-quantity flake from #42
  - **2026-08-17, second pass**: rate stayed this high (PR #50 hit it 5 times in a row before landing, always the full 20000ms timeout, never queueing) — investigated for real this time rather than deprioritized again. `@clickhouse/client`'s own troubleshooting docs (confirmed via context7) name the exact mechanism: Keep-Alive reuses idle sockets, the server closes ones past its own timeout, and the client can be unaware and try to reuse a dead one — normally caught by the client's own `idle_socket_ttl` timer, but that timer can fire late "on CPU-starved machines," which a shared CI runner running Postgres + ClickHouse + this whole suite concurrently genuinely is. Added `keep_alive.eagerly_destroy_stale_sockets` (the client's documented mitigation) + an explicit `request_timeout` (15s, below CI's 20s bun:test ceiling) as defense in depth. **Real, if still-early, evidence it helped**: PR #51 (rebased onto this fix) landed CI-green on the very first attempt — a genuinely different result from every PR in the several days prior. Not declared fully solved off one clean run; the next several PRs are the real test

---

## Code review findings addressed (2026-08-17)

A `/code-review high` pass across the full diff from PR #41 through #52 (six parallel angles: correctness, efficiency, simplification, reuse, conventions, and two independent line-by-line scans) surfaced 19 real findings, most-severe first. Being fixed incrementally, same discipline as everything else in this file — real verification each time, not just "the diff looks right."

- [x] **Query-param validation: `windowMinutes`/`limit` now real, typed, and enforced by Elysia itself** (findings 1, 2, 5)
  - [x] The bug: `query.windowMinutes ? Number(query.windowMinutes) : undefined` and the equivalent for `limit` on `issues`/`network`/`sessions`/`navigation` performed no validation — a non-numeric value produced `NaN` (not `undefined`, so default-parameter fallbacks never kicked in), which flowed into `toClickHouseDateTime64()` (`.toISOString()` on an Invalid Date throws `RangeError`) or a ClickHouse `UInt32` bind param (rejected, throws inside the query call) — either way, an uncaught exception that `errorHandlingPlugin` maps to a blanket `503 DEPENDENCY_UNAVAILABLE`, mislabeling a client input error as a backend outage. A *negative* `windowMinutes` was worse: it parsed successfully and put `windowStart` in the future, so an actively-erroring application's own health check reported it as `"stale"` with error data zeroed out
  - [x] Fixed at the route boundary, not defensively re-checked deeper in the call chain: every affected query schema's `t.Optional(t.String())` became `t.Optional(t.Number({ minimum: 1 }))` — Elysia 1.1+ coerces `t.Number` for query/params schemas automatically (confirmed against current docs via context7, not assumed), so a non-numeric, zero, or negative value is now rejected with a clean `422` before the handler ever runs. No `maximum` added for `limit` — `db/issues.ts`'s own `Math.min(filters.limit ?? DEFAULT_LIMIT, MAX_LIMIT)` already clamps an oversized value intentionally, a different, pre-existing tradeoff this fix doesn't change
  - [x] 7 new regression tests (non-numeric/negative/zero `windowMinutes`, non-numeric `limit` on all four affected routes) — 226 control-api tests passing (was 219). `biome`/`tsc` clean
  - [x] **Real end-to-end verification against a live server**: registered a real org/project, confirmed `?windowMinutes=abc` and `?windowMinutes=-60` both `422`, a valid `?windowMinutes=30` still `200`s, and `?limit=abc` on issues `422`s — the exact requests that used to 503 or silently misreport
- [ ] Privacy redaction: multi-word secrets partially leak, nested breadcrumb data bypasses redaction entirely (findings 3, 4)
- [ ] Health dashboard's own time-window filter never wired up on the frontend (finding 6)
- [ ] Filtered empty states don't distinguish "no data ever" from "no data for this filter" (finding 7)
- [ ] `parseClickHouseTimeRangeQuery` silently drops an invalid `from`/`to` instead of a clean error (finding 8)
- [ ] `authorizeApplicationAccess`/`authorizeEnvironmentAccess`/`...WithFallback` triplicated auth logic (finding 9)
- [ ] Efficiency: sequential awaits in `applicationHealth.ts`, double-fetch in `applications.ts`/`environments.ts`, breadcrumb ring buffer O(n) copy, `applyPrivacy`'s rules array rebuilt every call (findings 10, 11, 15, 16)
- [ ] Reuse: `db/navigation.ts`'s filter-builder duplicated a 5th time, `formatPercent` duplicated + `HealthOverview` reaching into a component file, 8 route files hand-copying the same nav bar (findings 12, 13, 14)
- [ ] Simplification: SSR guard duplicated across 5 modules instead of centralized, 5 list components copy-pasting the same filter-state wiring, `ApplicationHealth`'s invariant living only in a comment instead of the type system (findings 17, 18, 19)

---

## Deviations log

- **2026-08-14, Step 6**: Issue list endpoint built as `GET /api/v1/projects/:projectId/issues` instead of `api-contracts.md`'s documented `GET /api/v1/applications/{id}/issues`. Reason: `Application` isn't a real entity yet (Step 2's deliberate nullable-FK shortcut on `projects`), and `project_id` is what `events` in ClickHouse is actually keyed by. Revisit once `Application` is real (not currently scheduled).
- **2026-08-14, Step 7**: `GET /api/v1/projects/:projectId/network` isn't in `api-contracts.md` at all — written before Network existed as a real telemetry type. Followed the same project-scoped shape the Step 6 deviation above already established, for the same reason (project_id is what events are keyed by). Not a deviation from a documented endpoint, just a new one that should get folded into `api-contracts.md` whenever that doc gets revisited post-MVP.
- **2026-08-14, Step 7**: same as above for `GET /api/v1/projects/:projectId/sessions` and `GET /api/v1/sessions/:sessionId` — not in `api-contracts.md`, written before Session had a real backend view. The detail route follows the issue-detail route's flat, composite-id shape (`/sessions/:sessionId` where `sessionId` is `"{projectId}:{session_id}"`) rather than nesting under `/projects/:projectId/sessions/:sessionId`, for consistency with `/issues/:issueId`.
- **2026-08-14, Step 7**: same as above for `GET /api/v1/projects/:projectId/performance` — not in `api-contracts.md`, written before Performance had a real backend view.
- **2026-08-14, Step 7**: `data-model.md` §1's `Release` (depends on `Application`) and `Deployment` (`Release + Environment + Time`) entities collapsed into one project-scoped `releases` table (`deployedAt` folded directly onto the release row). Reason: `Application`/`Environment` aren't real tables yet, same gap `projects` itself already has (Step 2's nullable-FK shortcut) — one project already stands in for "one environment" the same way it does for every other Step 7 project-scoped endpoint. Revisit if/when `Application`/`Environment` become real (not currently scheduled) and a release genuinely needs to roll out to more than one environment.
- **2026-08-14, Step 7**: same as the Network/Session/Performance entries above for `GET /api/v1/projects/:projectId/releases/:version/health` — not in `api-contracts.md`, written before Release had a real health view.
- **2026-08-14, Step 8**: `/projects/:projectId/alert-rules` (POST/GET/PATCH) isn't in `api-contracts.md` — written before Alerting had a real backend. Project-scoped, same reasoning as every other Step 7/8 deviation above (`Application` isn't real yet, `project_id` is the real tenant boundary that exists). Alert type scope (`new_issue` only) and notification mechanism (webhook URL) were both confirmed with the user before building, not assumed — `api-contracts.md`/`E13-alerts.md` don't specify either.
