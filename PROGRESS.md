# FrontWatch — Build Progress

A personal build-tracking checklist, kept separate from `docs/`. The *plan* lives in [`docs/02-product/roadmap.md`](docs/02-product/roadmap.md) §2 and [`docs/07-delivery/execution-roadmap.md`](docs/07-delivery/execution-roadmap.md) — this file tracks actual status against that plan, plus notes on anything that changed along the way. **Order isn't fixed** — if a step gets reordered or reshaped, note it in the deviations log below rather than silently rewriting history.

Legend: `[ ]` not started · `[~]` in progress · `[x]` done

---

## Step 1 — Repository & local platform ✅

- [x] Monorepo skeleton (`apps/`, `packages/`, `services/data-plane/`) per `docs/06-engineering-specs/README.md`
- [x] Bun workspace root (`package.json` + `workspaces`), one placeholder `package.json` per package
- [x] Go module for `services/data-plane`, four `cmd/*` binary stubs (`ingestion`, `worker`, `alert-worker`, `retention-worker`), `go build ./...` compiles clean
- [x] `.gitignore` covers Go build artifacts (`bin/`, `*.test`, `*.out`)
- [x] Local infra verified healthy (`infra/local/docker-compose.yml` — Postgres, ClickHouse, Redpanda)
- [x] Initial commits pushed to `github.com/AgiriTaofeek/FrontWatch`, default branch `main`
- [x] CI (`.github/workflows/ci.yml`): Biome check + Go build/lint on push and PR
- [x] Branch protection on `main`: PR required (0 reviewers, solo project), CI must pass, no force-push/delete, applies to admins too

**Done:** 2026-08-12 — this fully closes M0's "CI" exit criterion, which had been marked done prematurely in the previous entry.

**Workflow from here on:** branch → PR → CI green → self-merge. Direct pushes to `main` are no longer possible (protection is enforced, including for the repo owner).

---

## Step 1.5 — Engineering standards & tooling *(decided 2026-08-12, before Step 2 code starts)*

- [x] Router for `apps/control-api`: **Elysia** — [ADR-018](docs/decisions/ADR-018-elysia-control-plane-router.md)
- [x] TS/Bun lint + format: **Biome**, replacing ESLint+Prettier — [ADR-019](docs/decisions/ADR-019-biome-ts-tooling.md)
- [x] Go lint: `golangci-lint` (unchanged, decided independently)
- [x] Auth library decision **deferred to Step 9** — do not pull in better-auth or wire up Keycloak/OIDC during Step 2; `tech-stack.md` already names OIDC/Keycloak as the documented direction, revisit deliberately (possible ADR) if that changes
- [x] Biome + golangci-lint installed and verified working (`biome check .` clean, `golangci-lint run ./...` clean)
- [x] Elysia installed in `apps/control-api`, `@types/bun`, shared `packages/config/tsconfig.base.json`
- [x] CI workflow + branch protection on `main` (recorded under Step 1 above — CI applies repo-wide, not control-plane-specific)
- [x] Husky + commitlint (`commit-msg`, enforcing Conventional Commits) + lint-staged (`pre-commit`: Biome on staged TS/JSON, `go build`+`golangci-lint --new-from-rev` on staged Go)

**Done:** 2026-08-12

---

## Step 2 — Control plane *(minimal — project identity only, no auth/RBAC yet)*

- [x] DB tooling decided: **Drizzle ORM, `bun-sql` driver** — [ADR-020](docs/decisions/ADR-020-drizzle-bun-sql.md)
- [x] UUID primary keys decided — [ADR-021](docs/decisions/ADR-021-uuid-primary-keys.md)
- [x] `drizzle.config.ts` + schema file in `apps/control-api` (`src/db/schema.ts`)
- [x] `projects` table (Postgres) — FKs to `application_id`/`environment_id` left **nullable for now** (real model in `data-model.md` §1 requires them; full `Application`/`Environment` tables deferred, tracked shortcut not silent scope creep)
- [x] First migration generated + applied against local Postgres (`infra/local/docker-compose.yml`), verified via `psql`
- [x] A way to create a project and read back its ID — `POST /projects` + `GET /projects/:id` (`apps/control-api/src/routes/projects.ts`)
- [x] Tests written alongside this code — `projects.test.ts`, integration-layer (real local Postgres), CI runs them against a `postgres:16` service container
- [x] `apps/control-api/src/index.ts` — app entry point mounting the route

**Done:** 2026-08-12 — closes Step 2. No auth/RBAC yet (still correctly deferred to Step 9).

**Why here:** the SDK's `init()` and every telemetry event need a real `project_id`, not a placeholder — see the reasoning captured in conversation on 2026-08-12.

---

## Step 3 — SDK skeleton ✅

- [x] `init()` — config validation (never crashes on bad config, disables itself instead), dedup behavior, client creation (`client.ts`)
- [x] Context (release, environment, route, sessionId, userAgent — minimal, no UA parsing yet) (`context.ts`)
- [x] Event envelope creation — SDK-side shape only, no tenant IDs (those get resolved server-side at ingestion, per data-model.md §10) (`event.ts`)
- [x] Buffer (bounded, FIFO, count-based only — no byte-size bound or shutdown-flush yet) (`buffer.ts`)
- [x] Transport skeleton — batching, bounded exponential backoff, retries only 5xx/network failures not 4xx (`transport.ts`)
- [x] Privacy stub — no-op, but structurally present in the pipeline per ADR-007 (`privacy.ts`)
- [x] First real instrumentation: `errors.ts` — `window.addEventListener('error'/'unhandledrejection')`, thin wrapper over `captureException`
- [x] Tests: 11 passing (buffer, transport retry/backoff with mocked fetch, error-instrumentation with `happy-dom`)

**Done:** 2026-08-13. **Real finding:** a root-level `bun test` does not reliably apply a nested package's `bunfig.toml` (confirmed empirically — `happy-dom` never registered, `window is not defined`) — each package's tests must run scoped to that package, both locally and in CI. Fixed `ci.yml` accordingly (separate `apps/control-api` and `packages/sdk` test steps).

**Deferred, not forgotten:** resource-loading-failure capture, `captureMessage`/`addBreadcrumb`/`setUser`/`flush`/`close`, sampling, real UA parsing, byte-bounded buffer, shutdown flush, framework adapters.

---

## Step 4 — Ingestion

- [x] ADR-022: Go ingestion reads Postgres directly (read-only), resolving code-structure.md's explicitly-flagged open question
- [x] `packages/contracts`: wire types matching `api-contracts.md` §3-4 exactly (`WireEvent`, `IngestRequest`, `IngestResponse`), plus a fixture lifted straight from the doc's own example
- [x] Fixed a real drift: SDK's `transport.ts` was sending an invented shape (camelCase, no batch envelope, made-up `X-Frontwatch-Key` header) that didn't match the documented wire contract at all — now serializes via `packages/sdk/src/serialize.ts` → `@frontwatch/contracts`, standard `Authorization: Bearer <publicKey>`, `POST /ingest/v1/events`
- [x] Contract test: `serialize.test.ts` asserts the serializer's output against the spec-derived fixture directly, not just "it compiles"
- [x] Validate incoming payload against the versioned event contract (Go side) — `internal/telemetry`, pure domain layer, zero infra imports per code-structure.md's strict dependency direction. 7 table-driven tests passing, `golangci-lint` clean
- [x] `internal/platform/config` — typed startup config, exits clearly on missing `DATABASE_URL`/`REDPANDA_BROKERS` rather than starting partially-configured. Real finding: a test passed locally only because my shell had `DATABASE_URL` exported from a prior command — `t.Setenv` doesn't clear vars it wasn't told about — fixed by explicitly clearing it per-subtest instead of relying on ambient absence
- [x] Auth against project key (Go side) — `internal/storage/postgres`, one narrow `ProjectCredentialRepository.FindActiveByPublicKey`, `pgx` v5.10.0, per ADR-022. Disabled and nonexistent keys deliberately return the same error (no signal leaked either way). Integration-tested against real local Postgres (insert/query/cleanup), skips cleanly if `DATABASE_URL` unset
- [x] Real gap caught and fixed: `ci.yml`'s Go job only ever ran `go build` + `golangci-lint` — `go test` never ran in CI, so the Postgres integration test above would have passed locally forever and never actually run where it matters. Added a `postgres:16` service + Bun/drizzle-kit migration step to the Go job too (Go's tests need the real schema Bun owns)
- [x] Publish to Redpanda (`franz-go` v1.21.6) — `internal/queue`, publishes raw decoded events to `telemetry.raw`, keyed by `project_id` (per-project ordering, load spread across partitions). Normalization/fingerprinting stay downstream in the worker, not ingestion's job
- [x] **Real infra bug found and fixed**: `infra/local/docker-compose.yml`'s Redpanda only advertised itself at `redpanda:9092` (Docker-internal hostname) — any client running on the host (not in a container) could complete the initial bootstrap against `localhost:9092` but then hung trying to reach the *advertised* address for the actual produce/consume. Never caught before because the healthcheck runs `rpk` *inside* the container, and nothing had actually tried to produce/consume from the host until this. Fixed with a proper dual-listener config (internal for other containers, external on a distinct port for the host) — confirmed by hitting `context deadline exceeded` before the fix, sub-100ms after
- [x] CI: `services:` can't override a container's startup command (needed for Redpanda's required flags) — tried the official `redpanda-data/github-action`, found it genuinely broken (hardcoded to a Docker Hub image name — `vectorized/redpanda` — that no longer exists), replaced with a direct `docker run` using the same known-working image as local dev
- [x] `internal/telemetry/service.go` — `IngestService` orchestrating `Authenticate → Validate → durably enqueue`, matching `api-contracts.md` §3 exactly, including partial batch acceptance (one malformed event rejects only that event, not the whole batch). 5 tests with fake credential/publisher implementations
- [x] `cmd/ingestion/main.go` wired together for real — HTTP handler (thin, per `services.md`'s rule: parse + map response, no business logic), `Authorization: Bearer` parsing, graceful shutdown (SIGTERM → stop accepting → finish in-flight → close dependencies, bounded 10s timeout, per `operations.md`)
- [x] **Full end-to-end verification, not just unit tests**: real project created in Postgres, real server started, real `curl` requests covering all paths (valid batch, missing auth → 401, wrong key → 401, malformed body → 400, mixed valid/invalid batch → partial acceptance) — then confirmed the accepted events actually landed on Redpanda with correct content via `rpk topic consume`, and that the rejected event correctly never did

**Done:** 2026-08-13 — closes Step 4. The MVP golden scenario's first three hops (SDK → ingestion → Redpanda) are now real and verified end to end, not just individually tested.

**Deferred, not forgotten:** `/health/live`/`/health/ready` endpoints (operations.md names these explicitly — Step 8's job), ingestion metrics (`ingestion_requests_total` etc., also Step 8), rate limiting, privacy checks at the ingestion boundary (instrumentation.md mentions this but it's not built), topic provisioning (currently relies on Redpanda's auto-topic-creation — fine for MVP, not a production answer per ADR-013).

---

## Step 5 — Processing

- [x] **Scope clarified**: issue grouping/counts/the Issue entity itself are Step 6's job, not Step 5's — this step is consume → decode/validate → normalize (pass-through, no framework-specific shapes exist yet to normalize) → fingerprint → write raw events, no mutable state
- [x] `migrations/clickhouse/0001_create_events_table` — `golang-migrate` (v4.19.1) with the ClickHouse driver, same versioned/reviewable role `drizzle-kit` plays for Postgres. `events` table: `ReplacingMergeTree`, partitioned by `toDate(client_timestamp)` (data-model.md §7's literal recommendation), ordered by `(project_id, event_type, client_timestamp, event_id)`
- [x] **Real infra bug #3 found and fixed, same class as Redpanda's**: the official ClickHouse image disables network access entirely for the `default` user unless `CLICKHOUSE_USER`/`CLICKHOUSE_PASSWORD` is set — confirmed directly from its own startup log. ClickHouse had been unreachable from outside its own container this whole time; the `/ping` healthcheck never required auth so never caught it
- [x] `internal/issue/fingerprint.go` — SHA-256 of `exception_type + normalized_message`, truncated to 16 hex chars, versioned (`FingerprintVersion`) per the legacy fingerprinting spec's explicit requirement. Normalization strips numbers/UUIDs/hex/quoted strings — noise-resistance verified both in unit tests and end-to-end (two real events with different order IDs through the whole running pipeline produced the identical fingerprint)
- [x] `internal/storage/clickhouse` — `EventWriter`, `clickhouse-go` v2.48.0 (official client)
- [x] `internal/telemetry/process_service.go` — `ProcessEventService` (Normalize→Fingerprint only; persist is the worker's wiring concern, not this service's)
- [x] `cmd/worker/main.go` wired together — `franz-go` consumer group, manual per-record commit strictly in order (a plain loop, not `EachRecord`'s callback — needed the ability to `break` early on a persist failure, since Kafka's committed offset is a single watermark per partition, not a sparse set; committing a later record would have silently marked an earlier failed one as done too)
- [x] **Full end-to-end verification**: real project created, both ingestion and worker started for real, sent real events through `curl` → ingestion → Redpanda → worker → ClickHouse, confirmed via direct ClickHouse queries. Also verified the explicit "malformed event must never terminate the worker" requirement by publishing genuinely broken JSON straight onto the topic — logged clearly, worker kept running

**Done:** 2026-08-14 — closes Step 5. **Deferred, not forgotten:** bounded-concurrency record processing (currently sequential — simplest-correct, not yet a throughput bottleneck), retry-with-backoff before giving up on a persist failure (currently stops and waits for redelivery/restart), dead-lettering malformed/invalid records (currently logged and skipped), enrichment (browser family/device class — blocked on the same UA-parsing gap noted in Step 3), a second server-side privacy layer (instrumentation.md names this; neither layer is real yet, client or server).

---

## Step 6 — Error investigation *(closes the MVP build-gate slice)*

- [x] ADR-023: issues derived by query (`GROUP BY project_id, fingerprint` over `events`), no separate materialized table — resolves the Step 5 deferral. Resolution state (`E11.03`) explicitly out of scope, not in `PROGRESS.md`'s original Step 6 list or `mvp.md`'s golden scenario
- [x] ADR-024: TanStack Start for `apps/web` — resolves `tech-stack.md`'s open frontend-router question, also doubles as SDK dogfooding (Tier 1 adapter target)
- [x] Issue grouping — `apps/control-api/src/db/issues.ts`, the ADR-023 aggregation queries (`listIssues`, `getIssue`), verified against `@clickhouse/client` v1.23.1 (works fine under Bun)
- [x] Issue API (Bun control plane) — `GET /api/v1/projects/:projectId/issues`, `GET /api/v1/issues/:issueId`. **Deviation from api-contracts.md** (tracked, not silent): documented as `/applications/{id}/issues`, built as project-scoped instead — `Application` isn't a real entity yet (Step 2's nullable-FK shortcut), `project_id` is what `events` is actually keyed by
- [x] **Also fixed while wiring this up**: every control-plane route lacked the `/api/v1` prefix `api-contracts.md` documents (ingestion already had `/ingest/v1/`, this side never got the equivalent) — fixed at composition time in `index.ts`, not per route file, so route-level tests keep testing unprefixed paths directly
- [x] **Real bug caught only by starting the actual composed server**, not by either route file's isolated tests: Elysia's router rejects two different parameter names (`id` vs `projectId`) at the same URL position across merged route trees — `projects.ts`'s `/projects/:id` renamed to `/projects/:projectId` for consistency with `issues.ts`
- [x] **Real format gap caught by testing**: ClickHouse's `DateTime64` via JSONEachRow insert wants its own `YYYY-MM-DD HH:MM:SS.mmm` string format, not ISO-8601 with `T`/`Z` — didn't surface in Step 5 because Go's client uses the binary native protocol, not string parsing
- [x] **Full end-to-end verification through all three services**: real project created, ingestion + worker + control-api all started for real, a `curl` request matching `mvp.md` §3's exact golden scenario (release `4.2.0`, route `/transfer`) sent through ingestion → Redpanda → worker → ClickHouse → confirmed visible via the real issue list and issue detail endpoints
- [x] CI: added ClickHouse to the TS job too (separate runner from the Go job, needs its own migrated instance) — the migration step there needs `golang-migrate` (Go-based) even though it's nominally the "TypeScript" job, since that's the one tool this project uses for ClickHouse schema
- [x] `apps/web` bootstrapped for real — TanStack Start (ADR-024), first frontend code since Step 1. `@tanstack/react-query` as the "dedicated query layer" `application-architecture.md` requires, query keys include every filter (`["issues", projectId]`), 30s `staleTime` per its "cache short-lived, telemetry isn't immutable" rule
- [x] `packages/contracts/src/issues.ts` — the Issue API's response shapes shared between `control-api` and `web`, same lesson Step 4 already paid for once with the SDK/ingestion wire contract: one definition, not two that can quietly drift
- [x] Dashboard issue list — `/projects/$projectId/issues`, real server-rendered data (TanStack Start streamed the `useSuspenseQuery` result into the initial HTML with zero explicit loader wiring — confirmed by reading the raw SSR'd HTML, not assumed)
- [x] Dashboard issue detail — `/issues/$issueId`, links directly from the list
- [x] **Two real setup bugs found only by actually starting the dev server**, not by writing the code: (1) `vite.config.ts` failed to load — `@tanstack/react-start/plugin/vite` is ESM-only but was being `require()`'d, because `apps/web/package.json` was missing `"type": "module"` (lost when `bun add` populated it, present in the setup guide's example); (2) `packages/contracts/package.json` had no `main`/`types` field at all — a latent gap since Step 1 that only became a visible error once a second package (`web`, alongside `sdk`) started importing from it
- [x] Component tests too, not just E2E — `@testing-library/react` + `happy-dom` (same scoped-`bunfig.toml` pattern as `packages/sdk`). Found two real bugs in the tests themselves while writing them: `<Link>` needs actual `RouterProvider` context to render (a bare `QueryClientProvider` wrapper threw), and `findByText` throws instead of returning falsy when nothing matches (need `queryByText` for "should NOT be present" assertions)
- [x] **Full end-to-end verification through all four services**: real project, real `curl` matching `mvp.md` §3's exact golden scenario, confirmed by reading the actual server-rendered HTML from the running dashboard — both the issue list and issue detail pages, real data, no gaps
- [x] CI: `apps/web` gets its own scoped test step (same root-vs-nested `bunfig.toml` limitation from Step 3)

**Milestone: Step 6 is done. `mvp.md` §3's golden scenario is now real, end to end** — a browser error becomes a queryable, visible issue in a real dashboard, verified with real services and real HTTP requests at every layer, not simulated. This closes the MVP build-gate slice from `mvp.md` §1.

---

## Step 7 — Context

- [~] Network — **SDK + Go pipeline done**, dashboard surface not yet
  - [x] `packages/contracts` + SDK `event.ts`: `EventType`/`WireEventType` extended to a real discriminated union (`error` | `network`) — the refactor Step 4's own comment said to do "when a second event_type is real"
  - [x] `packages/sdk/src/network.ts`: automatic `fetch` capture. Resource normalization mirrors the backend fingerprint normalization's *idea* (strip numeric/UUID segments, drop query strings) in a different language — `/api/users/123` → `/api/users/:id`. XHR instrumentation deferred, not forgotten (materially different API shape, real separate work)
  - [x] **Real gap caught while adding this**: `core-architecture.md` says `init()` is supposed to "register instrumentation" itself — ours never did, `registerErrorInstrumentation()` had to be called manually this whole time. Fixed by auto-registering both error and network instrumentation inside `init()`, with a dedicated registration guard (separate from `client.ts`'s own duplicate-init guard) so a second `init()` call can't double-wrap `window.fetch`
  - [x] **Real architectural constraint respected, not worked around**: `errors.ts`/`network.ts` both import from `client.ts`; auto-registration logic lives in `index.ts` (not `client.ts`) specifically to avoid a circular import that moving it into `client.ts` would have created
  - [x] Never captures the SDK's own telemetry requests as network events (would otherwise generate infinite low-value "network event about sending network events" noise) — `ignoreUrlPrefix` excludes the configured ingestion endpoint
  - [x] **Real Bun testing gotcha found and documented**: `mock.module()` replaces a module in the *shared* registry for the whole test run, not scoped per file — `errors.test.ts`'s mock of `./client` (only stubbing `captureException`) broke `network.test.ts` when run together, since the mocked module didn't have `captureNetworkEvent`. Fixed by keeping every file's mock of a shared module complete, not just what that one file uses
  - [x] 24 sdk tests passing (was 13), including 3 full-suite runs to confirm the mock-ordering fix actually holds, not just once
  - [x] Go `internal/telemetry.Event`: `Payload ErrorPayload` (a single concrete struct) replaced with a real discriminated shape — `ErrorPayload *ErrorPayload` / `NetworkPayload *NetworkPayload`, exactly one set per `EventType`, mirroring the TS union. `encoding/json` can't pick a struct type from a sibling field on its own, so `Event` grew its own `MarshalJSON`/`UnmarshalJSON` (shared via one `eventWire` struct) rather than pushing every consumer into runtime type assertions
  - [x] `Event.Validate()` now branches per `EventType`: error events require `payload.message`, network events require `payload.method` + `payload.resource`; a genuinely unsupported `event_type` (e.g. `performance`) is still rejected the same as before
  - [x] `ProcessEventService.Process()` only fingerprints error events (unchanged behavior, now guarded on `event.ErrorPayload != nil` too); `EventWriter.WriteEvent()` uses a new `Event.PayloadJSON()` helper instead of assuming one payload shape — same helper backs both `MarshalJSON` and the ClickHouse write path, so the switch-on-`EventType` logic exists in exactly one place
  - [x] **Real end-to-end verification, not just unit tests**: ran ingestion + worker against the real local Postgres/Redpanda/ClickHouse, `curl`'d a real network event through `/ingest/v1/events`, confirmed it landed in ClickHouse with the correct `payload` JSON via a direct HTTP query — plus a regression check that error events and genuinely-unsupported `event_type`s still behave exactly as before
  - [x] **Real bug found during that E2E pass, not a code bug**: stale `go run ./cmd/worker` processes left running from earlier Step 5/6 testing were still members of the `telemetry-worker` consumer group, running the *old* pre-network-support code. Since the dev topic has one partition, one of those old processes silently won the assignment, decoded the network event with the old single-`ErrorPayload` shape (getting zero values, not a decode error), failed `Validate()`, and committed past it as a permanent rejection — so the first E2E attempt showed nothing in ClickHouse with no error logged anywhere. Found by checking `ps aux` and the raw Redpanda topic (`rpk topic consume`) directly rather than assuming the new code was wrong; fixed by killing every stale process before retesting
  - [x] New tests: `TestEvent_JSONRoundTrip` (error + network, both directions, plus a raw-JSON shape check), `Validate()` table cases for both payload types, `ProcessEventService` skips fingerprinting for network events, `TestEventWriter_WriteEvent_NetworkEvent` (real ClickHouse integration test), `IngestService` accepts a valid network event — all passing, `golangci-lint run ./...` clean
  - [x] Dashboard surface: `GET /projects/:projectId/network` (control-api) aggregates by `(method, resource)` — same ADR-023 GROUP-BY-over-raw-events shape as issues, no materialized table. `duration_ms`/`outcome` live inside the raw `payload` JSON string column (ADR-008), pulled out per-row with ClickHouse's `JSONExtractString`/`JSONExtractFloat` since the aggregation (count/quantile) has to run in ClickHouse anyway — request count, failure count/rate, p50/p95 duration via `quantile()`
  - [x] `packages/contracts/src/network.ts` — same shared-contract pattern as `issues.ts`, one definition for control-api + web
  - [x] `apps/web`: `/projects/:projectId/network` route + `NetworkList` — same `useSuspenseQuery` pattern as `IssueList`, no per-resource detail page (deliberately deferred — no natural drill-down target the way an issue has occurrences). Small cross-links added between the Issues and Network pages since both are now real
  - [x] **Real end-to-end verification, full stack**: ran ingestion + worker + control-api + the actual `vite dev` dashboard together (not mocked at any layer), sent 4 real network events (3 success, 1 failure, varied durations) through `/ingest/v1/events`, confirmed the aggregation via a direct `curl` to the new endpoint, then confirmed the same numbers rendered in the real server-rendered HTML from the running dashboard (25.0% failure rate, 103ms p50, 271ms p95 — matching hand-computed values for the 4 seeded events)
  - [x] New tests: `listNetworkResources` (groups by method+resource, keeps different methods on the same resource separate, ignores error events, respects a route filter, empty-project case) against real ClickHouse; route-level tests for `GET /projects/:projectId/network`; `NetworkList` component tests (empty state, row rendering, separate rows per method) — all passing, `golangci-lint`-equivalent (`biome check` + `tsc --noEmit`) clean for both `apps/control-api` and `apps/web`
- [x] Session — SDK lifecycle, control-api views, and dashboard, all done
  - [x] `packages/sdk/src/context.ts`: session id goes from "a random UUID for the SDK's in-memory lifetime" to a real lifecycle — persisted in `sessionStorage` (survives a page reload within the same tab, which a pure module-level variable never did) and expires after 30 minutes of inactivity (industry-standard default, e.g. GA4/Sentry both use this), extended on every event rather than fixed at session start. Multi-tab is still explicitly out of scope — `sessionStorage` is per-tab by design, an acceptable approximation, not solved here
  - [x] Graceful fallback chain, never throws: storage genuinely unavailable (SSR, disabled) → in-memory fallback identical to the old skeleton behavior; storage accessible but `setItem` throws (Safari private browsing) → same in-memory fallback for the rest of the page load; corrupted/malformed stored JSON → treated as no session, a new one is created
  - [x] **Real, previously-invisible gap found and fixed while verifying this**: `packages/sdk` had *no* `@types/bun` dependency at all — `tsconfig.base.json` requires `"types": ["bun"]`, but nothing actually provided it, and it was never caught because CI only ever runs `bun test` (which strips types and doesn't check them) with no `tsc --noEmit` step anywhere. `packages/contracts` had the identical gap; `apps/control-api` had it declared individually; `apps/web` sidesteps it entirely via its own `"types": ["vite/client"]` override. Fixed by hoisting `@types/bun` to the root `devDependencies` once (matching how `biome`/`husky`/`lint-staged` are already root-only) instead of duplicating it per package, and removing `control-api`'s now-redundant individual copy
  - [x] **Two more real type errors surfaced the moment `@types/bun` actually resolved for `packages/sdk`** (both pre-existing since the Network PR, #15, invisible until now): `network.test.ts`'s untyped `mock(() => {})` made `.mock.calls` infer as `[][]` (empty tuples), silently typing every `call?.[0]` as effectively unreachable — fixed by typing the mock's parameter; `network.ts`'s `window.fetch = async (...) => {...}` stopped structurally matching `typeof fetch` once `@types/bun`'s Bun-specific `fetch.preconnect` augmentation became visible (real in Bun's own runtime, meaningless for a browser SDK) — fixed with a documented `as typeof fetch` cast
  - [x] **CI gap closed as a direct consequence**: added a `Typecheck` step per TS package (`contracts`, `sdk`, `control-api`, `web`) to `ci.yml`, right after Biome — the exact step whose absence let all of the above stay invisible this entire session
  - [x] New `context.test.ts` (context.ts had no tests at all before this): session id creation, reuse within the timeout window, persistence in `sessionStorage` (not just in-memory), activity-extends-the-session, expiry just past 30 minutes, no-expiry just under 30 minutes, recovery from corrupted stored JSON, and the full in-memory-fallback path (storage access itself throwing) — 9 new tests, 33 total in `packages/sdk` (was 24)
  - [x] `GET /projects/:projectId/sessions` + `GET /sessions/:sessionId` (control-api): same ADR-023 GROUP-BY-over-events shape as issues/network, no materialized Session table — `session_id, started_at, last_seen_at, event_count, error_count, network_count, first_route, last_route` via `min`/`max`/`countIf`/`argMin`/`argMax`, plus a chronological (oldest-first, unlike issue occurrences' newest-first) timeline query for detail, with a server-built human-readable `summary` per event (error message, or `METHOD resource -> status` for network). `browser`/`device` from `data-model.md`'s Session entity still aren't populated — needs real User-Agent parsing, which `context.ts`'s own comment already documents as deliberately not built
  - [x] Session id is a `"{projectId}:{session_id}"` composite (same pattern as `IssueSummary.issueId`) so `/sessions/:sessionId` is a flat, self-describing resource — not nested under project, matching the issue detail route's shape
  - [x] `apps/web`: `/projects/:projectId/sessions` (`SessionList`) + `/sessions/:sessionId` (`SessionDetail`, full timeline table). Three-way cross-nav added between Issues/Network/Sessions now that all three are real
  - [x] **`session-investigation.md`'s documented entry point wired up for real**: "Issue → affected session" — `IssueDetail`'s occurrence rows now link to `/sessions/:sessionId`, built client-side from the issue's own `issueId` prefix (`"{projectId}:{fingerprint}"`, split on the first colon) + the occurrence's raw `sessionId`, so no contract shape needed to change just to carry `projectId` separately
  - [x] **Real end-to-end verification, full stack**: ran ingestion + worker + control-api + the actual `vite dev` dashboard together, sent an error event and a network event sharing one `session_id` through `/ingest/v1/events`, confirmed the session list/detail endpoints via direct `curl`, then confirmed the identical numbers and full timeline (both events, correct order, correct summaries) rendered in the real SSR'd HTML — including following the Issue → session link end to end and confirming its `href` was exactly `{projectId}%3A{sessionId}`
  - [x] New tests: `listSessions`/`getSession` (groups error+network events on one session, chronological timeline with correct summaries, excludes events with no `session_id`, empty-project case) against real ClickHouse; route-level tests for both endpoints; `SessionList`/`SessionDetail` component tests; extended `IssueDetail`'s tests for the new session link (dash when absent, correct scoped `href` when present) — all passing, `biome check` + `tsc --noEmit` clean across all 4 TS packages
- [x] Performance — SDK, Go pipeline, and dashboard, all done
  - [x] `packages/sdk/src/performance.ts`: wraps the official `web-vitals` (v6.1.0) library's `onCLS`/`onFCP`/`onINP`/`onLCP`/`onTTFB` — instrumentation.md §Performance's first five metrics. Deliberately a thin adapter, not a reimplementation: `instrumentation.md`'s own hard requirement ("the instrumentation must itself be measured — it must not significantly distort the metrics it's trying to observe") is exactly why LCP/CLS/INP's real edge cases (layout-shift session windowing, back/forward-cache restores, soft navigations) are left to the library that already gets them right, not hand-rolled. Confirmed current API (`onCLS`/`onINP`/`onLCP`/`onFCP`/`onTTFB`, `Metric` shape) directly against the installed package's own `.d.ts` files, not just the README, since `INP` replaced `FID` as a Core Web Vital at some point and training data could be stale on that
  - [x] `EventType`/`WireEventType` extended to a three-member discriminated union (`error` | `network` | `performance`) — same "extend when a real third member exists" pattern Step 4/7 already established
  - [x] Navigation timing, resource timing, and long tasks — the rest of `instrumentation.md`'s Performance list — deliberately deferred, not forgotten, same "fetch alone covers the modern case, XHR is separate work" scoping `network.ts` already used. Sampling (`instrumentation.md`: "strong candidate for configurable sampling") also deferred — no volume problem exists yet to size a sampling strategy against
  - [x] Auto-registered inside `init()` alongside error/network instrumentation, same guard against double-registration on a repeat `init()` call
  - [x] New tests: `performance.test.ts` mocks `web-vitals` itself (its algorithms are Google's to test, not this SDK's) to verify `registerPerformanceInstrumentation()` wires up all five metrics and that the adapter maps `web-vitals`' `Metric` shape to `PerformancePayload` correctly; `event.test.ts` extended for `createPerformanceEvent`. 36 sdk tests passing (was 33), run 3x consecutively to confirm the shared-mock-registry pattern (`errors.test.ts`/`network.test.ts` had to grow a third stubbed export, same lesson as before) still holds
  - [x] Go `internal/telemetry.Event`: `PerformancePayload` (`MetricName, Value, Rating, NavigationType`) added as the struct's third discriminated payload variant — `Event.Validate()` requires `payload.metric_name` but deliberately *not* a non-zero `value` (a CLS value of `0` is a real "no layout shift occurred" measurement, not a missing one — the same zero-isn't-absent nuance web-vitals itself has to handle). `ProcessEventService`/`EventWriter` needed no code changes — both already switch generically on `EventType` via `PayloadJSON()`, so a third variant just worked
  - [x] **Real end-to-end verification, not just unit tests**: ran ingestion + worker against the real local Postgres/Redpanda/ClickHouse, `curl`'d a real performance event through `/ingest/v1/events`, confirmed it landed in ClickHouse with the correct `payload` JSON via a direct HTTP query — plus a regression check that error events and genuinely-unsupported `event_type`s still behave exactly as before
  - [x] **Real bug found during that E2E pass, not a code bug — same class as Network's #16, worse this time**: found *four* separate stale `go run ./cmd/worker`/`./cmd/ingestion` child binaries left running from earlier sessions' E2E testing (Network, Session), still bound to the shared dev Redpanda topic's single partition and to port 8081, running old pre-Performance code. `kill -9` on a `go run` wrapper's PID does **not** reliably kill its compiled child process — the actual leak mechanism, confirmed by watching a *fresh* `kill` immediately produce two more orphaned children from the very next `go run` invocation. Fixed for this session with `pkill -9 -f "go-build.*worker$"` / `...ingestion$"` instead of PID-based kills; worth remembering for every future manual E2E check in this repo
  - [x] New tests: `Validate()` table cases for `PerformancePayload` (missing payload, missing `metric_name`, a zero `value` explicitly passing), `TestEvent_JSONRoundTrip`'s third case, `ProcessEventService` skips fingerprinting for performance events, `TestEventWriter_WriteEvent_PerformanceEvent` (real ClickHouse integration test), `IngestService` accepts a valid performance event — all passing, `golangci-lint run ./...` clean
  - [x] `GET /projects/:projectId/performance` (control-api) aggregates by `metric_name` — same ADR-023 GROUP-BY-over-events shape as issues/network/sessions, no materialized table. `value`/`rating` live inside the raw `payload` JSON string column (ADR-008), pulled out per-row with ClickHouse's `JSONExtractString`/`JSONExtractFloat`. p50/p75 (not p95, unlike Network's durations) matches how Web Vitals are conventionally reported — CrUX/PageSpeed both score a page on its 75th percentile. No `limit` param unlike issues/network/sessions: there are only ever five possible `metric_name` values, so `GROUP BY metric_name` can never return more rows than that regardless of event volume
  - [x] `apps/web`: `/projects/:projectId/performance` route + `PerformanceList` — same `useSuspenseQuery` pattern as the other three. CLS formatted as a unitless score (`0.050`), the other four metrics as milliseconds (`1800ms`) — formatting CLS as "0ms" would be actively misleading, not just imprecise. Four-way cross-nav completed across Issues/Network/Sessions/Performance now that all four are real
  - [x] **Real end-to-end verification, full stack**: ran ingestion + worker + control-api + the actual `vite dev` dashboard together, sent 4 real performance events (3 LCP across all three ratings, 1 CLS) through `/ingest/v1/events`, confirmed the aggregation via a direct `curl` to the new endpoint, then confirmed the identical numbers (CLS `0.050`/`0.050`, LCP `2200ms`/`3350ms` p50/p75, `33.3%`/`100.0%` good rates) rendered in the real SSR'd HTML from the running dashboard
  - [x] New tests: `listPerformanceMetrics` (groups by metric_name, keeps different metrics separate, ignores non-performance events, respects a route filter, empty-project case) against real ClickHouse; route-level tests for the new endpoint; `PerformanceList` component tests (empty state, ms-formatted row, CLS-as-unitless-score row, separate rows per metric)
  - [x] `biome check` + `tsc --noEmit` clean across all 4 TS packages. All unit + integration tests passing
- [x] Release — Postgres entity, control-api CRUD, health aggregation, and dashboard, all done
  - [x] Real gap this sub-item needed that Context/Network/Session/Performance didn't: those four are all telemetry-shaped (a new `event_type` flowing through the existing SDK -> ingestion -> ClickHouse pipeline), but Release is a control-plane concept (a deployment record), not telemetry — the SDK already sends a free-text `release` string on every event today (Step 4's `SdkConfig.release`, stored as ClickHouse's `release` column), so no SDK or Go work was needed here at all, only a new Postgres table + control-api endpoints
  - [x] New `releases` table (Drizzle schema + migration): `id, project_id, version, commit_sha, deployed_at, created_at`, unique on `(project_id, version)`, `project_id` a real enforced foreign key to `projects.id` — the first actual FK anywhere in this schema (`projects.application_id`/`environment_id` are deliberately unenforced, Step 2's nullable-FK shortcut)
  - [x] **Deviation, logged, not silent**: `data-model.md` §1 has `Release` depend on `Application` and a separate `Deployment` entity (`Release + Environment + Time`, for one release rolling out to multiple environments) — neither `Application` nor `Environment` exist as real tables yet, same gap `projects` itself already has. Collapsed `Release`+`Deployment` into one project-scoped row (`deployedAt` folded directly onto `releases`) rather than building two tables around entities that don't exist — one project already stands in for "one environment" the same way it does for every other Step 7 project-scoped endpoint
  - [x] `POST /projects/:projectId/releases` (create, `onConflictDoNothing()` + a real `409` on a duplicate `(project, version)` — not a raw Postgres constraint-violation error) + `GET /projects/:projectId/releases` (list, newest `deployedAt` first — matches `release-investigation.md`'s "Releases → select release" flow, the just-shipped release is the one most likely being investigated)
  - [x] **Real end-to-end verification, live server, not just in-process route tests**: started `control-api` for real, `curl`'d a project + two releases (one with an explicit backfilled `deployedAt`) + a duplicate-version attempt + the list endpoint, confirmed every response and the underlying Postgres rows directly via `psql`
  - [x] New tests: create with generated id, defaults (`commitSha` null, `deployedAt` now) when omitted, explicit `deployedAt` respected, duplicate `(project, version)` returns `409` not a crash, list ordered newest-first (proven via a deliberately-backfilled-old release, not insertion order), empty list for a project with none — all against real local Postgres, same integration-test pattern `projects.test.ts` already established
  - [x] `GET /projects/:projectId/releases/:version/health` (control-api): joins the Postgres `releases` row against ClickHouse events filtered to `release = version` — error count, distinct-fingerprint issue count (`uniqExactIf(fingerprint, fingerprint != '')`, "how many things broke," not "how many times"), network request/failure counts + rate, and a full performance-metrics breakdown. Reuses `listPerformanceMetrics` (extended with a new `release` filter, alongside `listNetworkResources`) rather than duplicating its quantile logic — only the error/issue and network aggregates needed new queries, since network's own `listNetworkResources` groups by `(method, resource)`, a finer breakdown than release health needs
  - [x] `apps/web`: `/projects/:projectId/releases` (`ReleaseList`) + `/releases/:releaseId` (`ReleaseHealthView`, `releaseId` a `"{projectId}:{version}"` composite — same pattern as `IssueSummary.issueId`/`SessionSummary.sessionId`). Five-way cross-nav completed across Issues/Network/Sessions/Performance/Releases now that all five are real
  - [x] **Real bug found via the live dashboard, not caught by any test**: the release detail page was first built as `/projects/:projectId/releases/:version`, nested under the existing `/projects/:projectId/releases` list route by file-based-routing convention — TanStack Router made the list route an implicit layout parent, which needs to render an `<Outlet />` for a nested child to ever appear. It didn't, so the detail URL matched successfully (confirmed in the router's own internal match data) but rendered the *list* page's content, not the detail page's — silently, no error anywhere. Found only by actually curling the running dev server and reading the returned HTML, not by any test (a `QueryClientProvider`-only component test never exercises real route nesting). Fixed by making the detail page a flat top-level route (`/releases/$releaseId`) instead, matching how `issues.$issueId.tsx`/`sessions.$sessionId.tsx` already avoid this exact trap — confirmed against TanStack Router's own docs (a `_` suffix escape hatch exists too, but the flat-route pattern already established by Issue/Session detail was the more consistent fix)
  - [x] **Real end-to-end verification, full stack**: ran ingestion + worker + control-api + the actual `vite dev` dashboard together, registered a real release via `POST`, sent one real error + network + performance event through `/ingest/v1/events` tagged with that release, confirmed `/releases/:version/health` via direct `curl`, then confirmed the identical numbers rendered in the real SSR'd HTML from the running dashboard — including the list page's link and the fixed detail page
  - [x] New tests: `getReleaseHealth` (joins Postgres + ClickHouse correctly, distinct-fingerprint issue counting, zero-network-telemetry case returns `0` not `NaN`, null for a nonexistent release) against real Postgres + ClickHouse; route-level tests for the health endpoint (200 with all-zero telemetry, 404 for an unknown version); `release` filter added to `listNetworkResources`/`listPerformanceMetrics` with new test coverage; `ReleaseList`/`ReleaseHealthView` component tests — all passing, `biome check` + `tsc --noEmit` clean across all 4 TS packages

**Milestone: Step 7 is done.** Context, Network, Session, Performance, and Release are all real, verified end to end at every layer (SDK where applicable, Go ingestion where applicable, control-api, dashboard). `mvp.md` §2's full hypothesis — correlating errors with sessions, network, performance, and releases — is now real, not just the build-gate slice Step 6 closed.

---

## Step 8 — Operations

- [ ] Alerts (new issue, error spike, performance degradation, health degradation)
- [ ] Internal observability (FrontWatch monitoring itself)
- [ ] Backup / restore

---

## Step 9 — Hardening

- [ ] Full auth / RBAC (Administrator / Engineer / Viewer)
- [ ] Tenant isolation
- [ ] Load testing
- [ ] Failure recovery
- [ ] Disaster recovery testing

---

## Step 10 — Pilot

- [ ] Real, controlled customer application onboarded
- [ ] Pilot readiness checklist satisfied (`docs/02-product/roadmap.md` §3)

---

## Deviations log

- **2026-08-14, Step 6**: Issue list endpoint built as `GET /api/v1/projects/:projectId/issues` instead of `api-contracts.md`'s documented `GET /api/v1/applications/{id}/issues`. Reason: `Application` isn't a real entity yet (Step 2's deliberate nullable-FK shortcut on `projects`), and `project_id` is what `events` in ClickHouse is actually keyed by. Revisit once `Application` is real (not currently scheduled).
- **2026-08-14, Step 7**: `GET /api/v1/projects/:projectId/network` isn't in `api-contracts.md` at all — written before Network existed as a real telemetry type. Followed the same project-scoped shape the Step 6 deviation above already established, for the same reason (project_id is what events are keyed by). Not a deviation from a documented endpoint, just a new one that should get folded into `api-contracts.md` whenever that doc gets revisited post-MVP.
- **2026-08-14, Step 7**: same as above for `GET /api/v1/projects/:projectId/sessions` and `GET /api/v1/sessions/:sessionId` — not in `api-contracts.md`, written before Session had a real backend view. The detail route follows the issue-detail route's flat, composite-id shape (`/sessions/:sessionId` where `sessionId` is `"{projectId}:{session_id}"`) rather than nesting under `/projects/:projectId/sessions/:sessionId`, for consistency with `/issues/:issueId`.
- **2026-08-14, Step 7**: same as above for `GET /api/v1/projects/:projectId/performance` — not in `api-contracts.md`, written before Performance had a real backend view.
- **2026-08-14, Step 7**: `data-model.md` §1's `Release` (depends on `Application`) and `Deployment` (`Release + Environment + Time`) entities collapsed into one project-scoped `releases` table (`deployedAt` folded directly onto the release row). Reason: `Application`/`Environment` aren't real tables yet, same gap `projects` itself already has (Step 2's nullable-FK shortcut) — one project already stands in for "one environment" the same way it does for every other Step 7 project-scoped endpoint. Revisit if/when `Application`/`Environment` become real (not currently scheduled) and a release genuinely needs to roll out to more than one environment.
- **2026-08-14, Step 7**: same as the Network/Session/Performance entries above for `GET /api/v1/projects/:projectId/releases/:version/health` — not in `api-contracts.md`, written before Release had a real health view.
