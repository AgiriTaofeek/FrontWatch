# Engineering Milestones

## M0 — Architecture Foundation

Outcome:

```text
repositories
build system
local environment
CI
coding standards
observability foundation
```

## M1 — First Telemetry

Outcome:

```text
SDK → ingestion → queue → storage
```

A test frontend event becomes queryable.

## M2 — First Investigation

Outcome:

```text
health → issue → occurrence
```

An engineer can detect and inspect a frontend error.

## M3 — Operational Context

Outcome:

```text
session
network
performance
release
```

An issue can be investigated with surrounding evidence.

## M4 — Production Hardening

Outcome:

```text
security
RBAC
tenant isolation
load
failure recovery
backups
```

## M5 — Pilot Ready

Outcome:

```text
installable
documented
observable
supportable
```

## M6 — General Availability

Outcome:

```text
stable release
upgrade path
support model
production SLOs
```
