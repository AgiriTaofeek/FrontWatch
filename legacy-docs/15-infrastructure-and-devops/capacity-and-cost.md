# Capacity & Cost Planning

## Main Drivers

```text
telemetry volume
retention
replication
query concurrency
storage performance
cluster size
```

Collect:

```text
users
peak concurrency
events/session
average event size
sessions/day
retention
applications
environments
```

Telemetry storage will often dominate infrastructure cost.

Sampling can control both browser/network load and cost, but critical error visibility must remain strong.

Future deployment tiers should be based on measured capacity.
