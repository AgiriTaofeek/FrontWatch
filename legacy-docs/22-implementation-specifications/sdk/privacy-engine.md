# SDK Privacy Engine

Default-deny sensitive capture where practical.

Support:

```text
header filtering
URL/query sanitization
input masking
custom-field filtering
metadata redaction
```

Privacy rules should be deterministic and testable.

Server-side redaction remains mandatory as defense in depth.
